# 虚空寄生实施方案计划

> - 本文档是《deepseek-harness系统分析.md》第 17 章结论的落地计划，是本方案唯一的进度跟踪来源。
> - 关联文档：`deepseek-harness系统分析.md`（第 16 章为"自建 Cordis"备选路线，第 17 章为"以 dsh 为基座"首选路线）。
> - 基座：`E:\project\star-sanctuary\Void\deepseek-harness-master`（`0.1.0-rc.5`，源码仓库 https://github.com/deepseek-ai/deepseek-harness.git；仅 `master` 分支、无 tag；已更新为 git 仓库并检出 `master@47f9438`，工作区干净）。
> - 复用来源：`E:\project\star-sanctuary`（Star Sanctuary，只读，零修改）。
> - Void 仓库：`E:\project\star-sanctuary\Void`（独立 git 仓库，物理放在 star-sanctuary 目录下，便于同屏查看两项目代码）。
> - 官方技术文档：https://deepseek-harness.github.io/deepseek-harness/develop/cordis-tutorial/

## 1. 方案定位与结论摘要

**Void 不是独立产品，而是寄生在 DeepSeek Harness（dsh）之上的一套树外插件组 + 一个深改 UI。**

最终产品形态：

```text
Star WebChat（深改布局，不新增功能模块）
              │  dsh SDK / JSON-RPC（替代 Star Gateway RPC）
              v
   dsh 宿主（headless / JSON-RPC / ACP）
   ├─ dsh 现成能力：agent-loop、session、tools、subagent、workflow、UI 运行时、CLI/SDK
   └─ Void 插件组（树外，寄生接入）：
        ├─ void-memory   ← Star belldandy-memory（FTS5 + sqlite-vec 知识层）
        ├─ void-tools    ← Star belldandy-skills（注册到 ctx.tools）
        ├─ void-legion  ← Star 军团（公开 ctx.voidTeam：花名册+汇报关系+团队模式+共享上下文；执行复用 orchestrator/launch-spec/delegation-protocol/team-identity-governance）
        ├─ void-channel-*← Star belldandy-channels（Feishu 等）
        ├─ void-browser  ← Star belldandy-browser（CDP relay）
        └─ void-security ← Star pairing/auth/allowlist（策略层）
```

核心结论（已与用户确认）：

1. **基座用法**：树外插件仓库 + 依赖已发布的 `@deepseek-ai/dsh-*`，基座 checkout 完全不动。
2. **正确接入形态**：Star 的团队/军团做成公开的 `ctx.voidTeam`（花名册 + 汇报关系 + 团队模式 + 共享上下文），**不替换** dsh 的 agent-loop、也**不降级为 subagent provider 黑盒**；Star 记忆做成 knowledge 层，**不双写** dsh 的 session；工具/子智能体/UI 直接挂 `ctx.tools` / `ctx.subagents` / UI slots。
3. **UI**：深改是**一等交付项**——基于 Star 自己的 WebChat 功能模块（不新增模块），重做布局与表现，并把传输层从 Star Gateway RPC 换成 dsh SDK/JSON-RPC。

## 2. 关键架构事实（勘察确认，写入以固化）

| 事实 | 结论 | 对 Void 的含义 |
| --- | --- | --- |
| `ctx.agentLoop` 单例，`ctx.agents.setFactory()` 已注册即抛错 | agent-loop 只能有一个 | Star 不能并存自己的 loop，只能走 subagent provider / 远端 |
| `ctx.subagents` 命名多提供方注册表 | 子智能体后端可共存 | 军团成员可作为 dsh 子智能体运行；团队拓扑/权威走 `ctx.voidTeam` |
| `ctx.tools` 作用域化工具注册表 | 工具可叠加 | belldandy-skills 工具与 dsh 工具共存 |
| `ctx.sessions` 唯一追加式 `SessionEvent` 日志真源 | 会话只能有一个真源 | Star 记忆作为其上的知识层，不双写 |
| 组合 = profile → bundle → cordis.patch.yml → home → --patch，按 id 整段替换/插入 | 可整体开/关或替换任一行 | Void 用独立 profile/bundle 承载 |
| dsh 无 FTS5/sqlite-vec 向量检索与知识记忆（仅 opt-in 会话全文检索 + spill） | Star 记忆是纯增量差异化 | `void-memory` 是最干净的新 capability seam |
| 能力 seam 必须三角色完整（Service Definition / Provider / Consumer），注册是 effect 且 HMR-safe | 接入门槛高于裸 Cordis | 每个 Star 能力按 seam 契约完整设计 |

## 3. Star 能力差异化分层

| 类别 | Star 模块 | 处理方式 |
| --- | --- | --- |
| 冗余（dsh 已有且更成熟） | agent-loop、ConversationStore、todo/plan/goal/subagent/workflow、bash/pwsh/fs/glob/grep、web search、compaction、skill | 不移植或薄适配，直接用 dsh |
| 纯增量差异化 | `belldandy-memory`（sqlite-vec、indexer、chunker、reranker、adaptive-retrieval、dream-*、memory-tree-*、external-memory-ingest、obsidian-sync） | 新建 `void-memory` knowledge seam，最高优先级 |
| 差异化军团 | 团队拓扑 + 权威图（`delegation-protocol` / `team-identity-governance`）+ 执行引擎（`orchestrator` / `launch-spec`） | 新建 `void-legion` 公开 `ctx.voidTeam` seam，最高优先级（与 memory 并列） |
| 差异化传输/宿主 | `belldandy-channels`（Feishu 等）、`belldandy-browser`（CDP relay） | 新 host/adapter 或新 capability seam |
| 差异化安全 | pairing/auth/allowlist（belldandy-core） | 评估 dsh 的 identity/credentials/permission/approval，不足则作新 seam |
| 差异化工具 | `belldandy-skills` 的 doctor、subtask detail 等 | 按需注册到 `ctx.tools` |
| 差异化 UI | Star WebChat（功能模块不新增，布局/表现强化） | 独立 UI 深改轨道，寄生 dsh SDK |

## 4. Void 插件形态（Star 模块 → dsh seam）

| Void 插件 | dsh 承载点 | 角色 | Star 代码来源 |
| --- | --- | --- | --- |
| `void-memory` | 新 `ctx.voidMemory` Service Definition + SQLite Provider + `memory_*` Consumer 工具 | 新 capability seam | `belldandy-memory`（store / indexer / reranker / dream / tree） |
| `void-tools` | `ctx.tools` 注册 + `tools/*` waterfall + `ctx.sandbox`/`ctx.permission` | Consumer + 策略 | `belldandy-skills`（executor / security-matrix / runtime-policy / faqi） |
| `void-legion`（军团） | 新 `ctx.voidTeam` Service Definition + Provider + `launch_legion` Consumer 工具 + 组织图 UI slot | 新 capability seam | 拓扑/权威：`delegation-protocol` + `team-identity-governance`；执行：`orchestrator` + `launch-spec`（belldandy-agent） |
| `void-channel-feishu` | 新 host/adapter + MessageSource | 宿主/传输 | `belldandy-channels`（feishu） |
| `void-browser` | 新 `ctx.browser` seam + 工具 | 新 capability seam | `belldandy-browser`（CDP relay） |
| `void-security` | 复用/扩展 credentials/permission/approval | 策略 provider | `belldandy-core`（pairing/auth/allowlist） |

## 5. 分阶段实施计划

### 阶段 0：方向澄清与静态评估（已完成）

- 明确"以 dsh 为基座、寄生融合"取代"自建 Cordis"，并确认正确接入形态（团队/军团→公开 `ctx.voidTeam` seam、记忆→knowledge 层、不双写 session、UI 深改一等交付）。
- 产出：`deepseek-harness系统分析.md` 第 17 章 + 本文档。

### 阶段 1：dsh 扩展契约 Spike + 树外 Void 脚手架

- 目标：吃透 dsh 契约并跑通"树外插件 + 独立构建 + profile 组装"的最小闭环。
- 内容：
  1. 建立 Void 树外仓库：pnpm/TypeScript ESM、tsconfig 双 face、tsdown 构建、vitest，声明 `dsh.profile`/`dsh.bundle`，依赖已发布 `@deepseek-ai/dsh-*`。
  2. 实现一个最小 capability seam（一个 Service Definition + 一个 Provider + 一个 Consumer 工具），通过 `ctx.effect`/`ctx.on` 注册，验证 HMR-safe 释放与独立构建。
  3. 验证 dsh 的 testing policy（REAL-composition 测试，非手搓 `ctx.plugin`）、per-package invariant、模型可见即已记录。
  4. 固定 dsh 精确版本/提交，建立升级兼容测试骨架。
- 预期效果：获得"树外插件如何被 dsh 装载、如何独立构建测试"的可复制模板。
- 完成条件：一个最小 Void 插件能在干净环境独立构建、测试、以 profile 方式装载并反向清理。

### 阶段 2：代表性 PoC（三样本）

- 目标：用真实 Star 模块验证寄生接入可行性与依赖闭包。
- 三样本：
  1. **MemoryManager → `void-memory`**：验证纯增量 knowledge seam 从零接入全流程；先验证"直接依赖 belldandy-memory 包"与"源码快照"两种方式的依赖闭包。
  2. **ToolExecutor → `void-tools`**：验证 Star 工具执行语义（security-matrix / runtime-policy / faqi）到 dsh 的 sandbox / approval / permission / scope-restriction 的映射，暴露 disposer 缺失与跨模块类型耦合。
  3. **军团 → `void-legion`**：用最小军团（规划→方案文档生成→写码→验收→进度记录回写五 lane，含 peer 关系 + 先后依赖）验证 `ctx.voidTeam` 公开 seam；执行复用 `orchestrator`/`launch-spec`，拓扑/权威复用 `delegation-protocol`/`team-identity-governance`；对照记录"若退化为 subagent provider 会失去拓扑/权威/共享上下文"。核心流程精确定义见本节末尾小节。
- 预期效果：得到每个样本的直接复用范围、必要补丁、传递依赖、迁移测试与未解决风险的真实数据。
- 完成条件：三样本均能被 Cordis 安装、使用、完整释放；失败注入后无残留；Star 仓库与 dsh 基座零改动；无指向开发机绝对路径的依赖。

#### 最小军团核心流程（精确定义）

核心流程共 5 条 lane，串行推进，后一条依赖前一条；验收不通过时回退到写码重做，直到通过。

| 顺序 | laneId | 身份 | 角色 | 对指挥官关系 | 依赖 | 产出 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | lane_plan | 规划 | researcher | peer | — | 任务拆解 + 验收标准（doneDefinition + verificationHints） |
| 2 | lane_plan_doc | 方案文档 | default | peer | lane_plan | 实施方案与计划文档（含末尾"实施计划进度表"骨架） |
| 3 | lane_code | 编码 | coder | subordinate | lane_plan_doc | 代码改动 + 自测结果 |
| 4 | lane_verify | 审计 | verifier | peer | lane_code | 验收结论（通过 / 不通过 + 证据） |
| 5 | lane_progress | 进度记录 | default | subordinate | lane_plan_doc、lane_code、lane_verify | 把进度回写到计划文档的"实施计划进度表" |

关键语义：

1. **方案文档是唯一权威**：lane_plan_doc 产出的计划文档是后续写码、验收、进度回写的唯一依据。写码只按文档实施，验收只按文档里的 doneDefinition / verificationHints 判定，杜绝"Agent 各干各的"漂移。
2. **进度回写既是人读心跳，也是机器续跑检查点**：lane_progress 不产出新功能，而是把 lane_code 与 lane_verify 的结果落到两处——(a) 写成类型化事件进入 dsh session 日志（权威、可恢复的真源）；(b) 渲染回写到计划文档的"实施计划进度表"（人读的唯一进度来源）。断线、意外关闭后，Agent 从日志重建各 lane 状态，只续跑未完成的 lane，避免大段重新查验与重复写码——这是本流程里最重要的两条文档 lane 之一。
3. **权威关系**：规划、方案文档、验收相对指挥官是 peer（协作 / 独立审计，互不从属）；编码、进度记录是 subordinate（只执行 / 记录，不指挥）。
4. **验收与编码平级**：verifier 不向 coder 汇报，独立判通过与不通过；不通过则 lane_code 重做，直到 lane_verify 通过，才进入进度回写。

落地到 Star 契约：lane 列表即 `DelegationTeamMetadata.memberRoster`（`DelegationTeamMember[]`），每条 lane 的 `role` / `identityLabel` / `authorityRelationToManager` / `dependsOn` 对应上表各列；团队模式取 `plan_execute_verify` 的扩展（在标准"规划→执行→验证"前增加"方案文档生成"、后增加"进度记录回写"）。`ctx.voidTeam` 把这套 roster 作为一等数据公开，组织图 UI slot 据此画出依赖链与汇报关系。

### 阶段 3：Void MVP

- 目标：交付一个可用的纵向闭环——记忆 + 工具 + 军团（公开团队 seam）+ 一个渠道，跑在 dsh 的 loop/session 上。
- 内容：`void-memory`（检索 + 写入 + 至少一条 dream/摄取链路）、一组 Star 工具、`void-legion`（公开团队 seam：花名册 + 汇报关系 + 最小军团编排 + 组织图）、`void-channel-feishu`（消息收发）、独立配置/数据目录/端口。
- 预期效果：用户能通过 dsh 的 session/UI（或 WebChat 初版）完成"问 → 查记忆 → 调工具 → 组建军团派活"的真实任务。
- 完成条件：达成第 8 节"Void MVP 完成标准"。

### 阶段 4：UI 深改（一等交付，可与阶段 5 部分并行）

- 目标：以 Star WebChat 功能模块为基础重做布局与表现，寄生在 dsh 宿主之上；**功能模块不新增**。
- 内容：
  1. **布局与表现强化**（可先行）：重做 Star WebChat 的信息层级、面板编排、视觉系统；保留 memory-viewer / subtasks / goals / workspace / doctor 等既有模块的 DOM 结构与数据语义，只改布局与视觉。
  2. **传输层适配**：把 WebChat 的 transport/RPC 从 Star Gateway 协议换到 dsh SDK/JSON-RPC；在 Void 插件侧补齐 WebChat 模块所需的 RPC 数据源（记忆、工具、目标、子任务等）的暴露。
- 风险：Star WebChat 的 DOM 接线与 settings 语义和 Star Gateway RPC 强耦合；dsh SDK 无 Star 的 goal/memory/dream/tools/workspace/subtask 方法，需要协议映射层。UI 深改中"布局重构"与"寄生接入"是两个可分离问题，后者依赖阶段 3 的宿主契约稳定。
- 预期效果：用户获得一个视觉与信息层级更优、但仍复用 Star 既有功能模块的 WebChat，其背后由 dsh 驱动。
- 完成条件：页面正常加载、无新增控制台错误、既有模块 DOM 接线可用，且数据来自 dsh 宿主而非 Star Gateway。

### 阶段 5：主要 Star 能力插件群

- 目标：补齐 Star 的差异化能力插件。
- 内容：`void-browser`（CDP relay）、更多渠道、`void-security`（pairing/allowlist 评估与接入）、dream/Obsidian 摄取完善、更多 belldandy-skills 工具。
- 预期效果：每批插件都有来源、生命周期、兼容测试与明确所有权。
- 完成条件：完成一批评估一批，不预先承诺全部迁移。

### 阶段 6：接近 Star 功能覆盖的独立产品

- 目标：仅在 Void MVP 与主要插件证明价值后评估。
- 内容：完整渠道、安全与恢复、安装升级、数据兼容决策、生产加固。
- 完成条件：需要独立产品范围、预算、数据兼容与生产验收标准（当前不作为首期完成条件）。

## 6. 风险等级与主要失败模式

总体风险 **中**（低于"自建 Cordis"路线），但对 dsh 基座变更的暴露上升。

| 失败模式 | 影响 | 早期信号 | 控制措施 |
| --- | --- | --- | --- |
| dsh 预发布 breaking change 直接波及插件 | 随基座升级频繁返工 | 契约随 `0.1.0-rc.*` 升级破坏 | 固定精确版本/提交，维护升级兼容测试，不跨大版本静默升级 |
| 误接成单例替换 | 失去 goal/ralph/todo/plan/compaction 或双真源 | 尝试替换 `agent-loop` 或并行写两套 session | 遵循第 4 节分层，loop/session 一律走注册表语义 |
| 团队拓扑真源不唯一 | 花名册/汇报关系两处记账，恢复与展示对不上 | `ctx.voidTeam` 与 orchestrator 各自存一份 roster | 拓扑真源定死一处（`ctx.voidTeam`），执行引擎只消费不另存 |
| 权威图无法展示/审计 | 军团退回黑盒，用户看不见谁指挥谁 | 界面没有组织图插槽 | PoC 先验证 roster/reportsTo/mayDirect 能经 UI slot 画出 |
| Star 工具执行语义与 dsh 策略层不一致 | 权限/hook/审批行为漂移 | 同一工具两处行为不一致 | 逐工具映射 security-matrix/runtime-policy → sandbox/approval/permission/restriction |
| 直接导入 Star 模块拉入大半个 Star | 依赖闭包失控 | 一个插件拖入大量未用包、`BELLDANDY_*` 环境变量 | PoC 输出传递依赖闭包，超范围改源码快照/原生移植 |
| 注册无法反向清理 | 重装后重复 | 重复创建 Context 行为次数增加 | 遵守 effect/HMR-safe 契约，补重复安装与故障注入测试 |
| dsh 严格契约抬高成本 | 真插件变重 | 在 invariant/typecheck/lint gate 反复返工 | 先吃透 docs/cookbook + packages/AGENTS.md，seam 一次设计到位 |
| UI 传输适配与布局重构耦合过深 | UI 轨道延期 | 布局改动与 RPC 改动相互阻塞 | 拆分"布局强化"与"RPC 适配"两个可分离子问题，后者等宿主契约稳定 |
| 复用代码来源失踪 | 无法判断同步状态 | 复制文件无来源提交 | 每插件维护来源清单、补丁说明、同步测试 |
| 命名空间/数据目录冲突 | 同机运行互相污染 | 复用 `DSH_*`/`BELLDANDY_*` 或默认目录 | 独立 `VOID_*` 命名空间、数据目录、端口、插件目录 |

## 7. 可行性前置条件

1. 确定 Void 发行形态：一个 `dsh.profile` 还是若干 `dsh.bundle`，及用户安装方式。
2. 固定 dsh 精确版本/提交，建立升级兼容测试；接受预发布 breaking change 成本。
3. 确定唯一状态模型：会话真源用 dsh 的 `SessionEvent` 日志；Star 记忆作为其上的知识层，不双写。
4. 吃透 dsh 契约：seam 三角色、`ctx.effect`/`ctx.on`、branded id、declaration merging、HMR-safe、模型可见即已记录、per-package invariant。
5. 建立 Star 来源机制：固定提交、来源清单、补丁目录、许可证/版权记录、同步流程。
6. 隔离运行命名空间：`VOID_*` 环境变量、独立数据目录、端口、插件目录。
7. 建立测试迁移规则：复用实现时同步评估相邻测试，在 Void 模块接口处补生命周期与故障测试（dsh 要求 REAL-composition 测试）。
8. 确定安全分层：受信任进程内 Cordis 插件与不可信外部扩展（OCI/远端）走不同路径。

## 8. 闭包边界与完成标准

### 本方案包含

- 以 dsh 为基座、树外 Void 插件组承载 Star 差异化能力；
- 遵守 dsh 的 seam/组合/生命周期契约，Star 零修改、基座零改动；
- 复用方式三选一，配来源清单与同步机制；
- 独立 `VOID_*` 命名空间、数据目录、构建、测试、发布路径；
- Star WebChat 深改（布局/表现强化，功能模块不新增）作为一等 UI 交付。

### 本方案明确不包含

- fork 或修改 deepseek-harness 基座；
- 修改 Star 源码、配置、测试或运行方式；
- 替换 dsh 的 agent-loop 或 session 真源（保留 dsh 产品能力）；
- 新增 WebChat 功能模块（只做布局与表现强化）；
- 默认兼容 Star 既有数据目录、会话数据库与全部 RPC；
- 把每个 Star 文件或类机械转换成独立插件；
- PoC 阶段即迁移 WebChat、全部渠道或完整 Gateway。

### Void MVP 完成标准

1. Void 在干净环境（无 Star 工作区、无绝对路径）独立安装、构建、测试、以 dsh profile 方式启动；
2. Star 工作树与 dsh 基座 checkout 全程无本任务改动；
3. 形成 Memory、Tools、Legion（公开团队 seam）三者的完整纵向闭环；
4. 插件可独立启停，失败按逆序释放，重复创建 Context 不产生残留（HMR-safe）；
5. 会话真源只有 dsh 的 SessionEvent 日志，Star 记忆不双写；
6. 每个 Star 来源插件记录来源提交、复用范围、Void 补丁、测试与同步状态；
7. 进程内 Cordis 插件仅限受信任代码，不可信扩展仍走隔离；
8. 文档明确首版包含项、排除项、升级方式与已知分叉。

## 9. 粗略工作量估算

假设：Star 只读；Void 用 TypeScript/Node/pnpm 并依赖已发布 dsh 包；首阶段不兼容 Star 既有用户数据；有 2-3 名熟悉 Star 与 dsh 的工程师；估算为工程量级，非排期承诺。

| 范围 | 粗略工作量 | 主要产出 |
| --- | ---: | --- |
| dsh 扩展契约与测试策略 Spike | `1-2` 工程师周 | 树外脚手架、最小 seam、profile 组装、独立构建与 HMR-safe 验证 |
| 代表性 PoC | **累计 `4-8` 工程师周** | MemoryManager、ToolExecutor、最小军团（void-legion）三样本 + 依赖闭包数据 |
| Void MVP | **累计 `4-7` 工程师月** | 记忆 + 工具 + 军团（公开团队 seam）+ 一个渠道，跑在 dsh loop/session 上 |
| UI 深改（布局/表现 + 寄生接入） | **累计 `3-6` 工程师月** | Star WebChat 布局重做 + 传输层切到 dsh SDK，功能模块不新增 |
| 主要 Star 能力插件群 | **累计 `8-15` 工程师月** | browser CDP、更多渠道、pairing 安全、dream/Obsidian、更多工具 |
| 接近 Star 功能覆盖的独立产品 | **累计 `14-24` 工程师月** | 完整渠道、安全与恢复、安装升级、生产加固 |

说明：相比"自建 Cordis"，节省主要来自"宿主/loop/session/tools/subagent/workflow/UI/CLI/SDK 现成"；成本转移到"dsh 严格契约遵守 + Star 执行/安全语义映射 + 预发布 churn + UI 传输适配"。团队 seam（`ctx.voidTeam` 的拓扑/权威/共享上下文/组织图）是新增的最重能力面，其全模式（verify_swarm/plan_execute_verify 等）在"主要能力插件群"阶段完成。核心模块仍不能保证原样复用；PoC 前不能给出可信的"可复用代码百分比"。

## 10. 后续计划（本轮）

- 下一步：评审**第 11 节项目架构**，通过后再启动阶段 1（dsh 扩展契约 Spike + 树外 Void 脚手架）。
- 为什么先做它：它产出"树外插件如何被 dsh 装载、构建、测试、清理"的可复制模板，是阶段 2 三样本 PoC 和后续一切插件的地基；不做它，后续所有 Star 能力接入都会在 dsh 契约上反复试错。
- 当前还缺的关键闭环：尚未验证"树外仓库依赖已发布 `@deepseek-ai/dsh-*` 是否真的能独立构建并通过 dsh 的 REAL-composition 测试"（基座已固定为 `master@47f9438` / `0.1.0-rc.5`）。

## 11. 项目架构（已评审，2026-08-15）

> 本节是进入阶段 1 之前要敲定的架构蓝图；标注"待 Spike/PoC 敲定"的细节在阶段 1/2 用真实代码校准。Star 仓库与 dsh 基座 checkout 全程零改动，依赖方向 Void → dsh、Void → Star（只读复用），绝无反向。
>
> **已拍板（本轮确认）**：
> 1. 军团进度 = 一个真源（dsh session 日志）+ 两个投影（计划文档进度表）——见 11.4；
> 2. Star 模块统一用**源码快照**复用，不把旧负担（Belldandy 全局配置、`BELLDANDY_*` 环境、整仓依赖）带入新项目——见 11.5；
> 3. Star WebChat 深改独立成 `apps/void-webchat`（独立 app，非插件）——见 11.2。
> 4. `apps/void-webchat` = 独立桌面 app + **Tauri 2**（Rust 壳 + WebView 前端）——见 11.2 / 11.9。
> 5. 桌面客户端对接 = **Rust 管 sidecar + 前端走 HTTP/WS 网关**——见 11.9。
> 6. 双壳并存 = 壳 A（dsh React + void 薄 UI）+ 壳 B（Tauri 2 完整 UI），共享 RPC/真源，复杂 widget 优先 Web Component；开发顺序先壳 A 后壳 B——见 11.10。

### 11.1 顶层拓扑

三层，职责单向向下依赖：

```text
① 壳 B：Void 客户端（apps/void-webchat，Tauri 2 完整 UI）Star WebChat 深改：布局/表现强化，功能模块不新增
        │  HTTP/WS `/api`（Typert RPC，替代 Star Gateway RPC）
        v
② dsh 基座（不 fork，依赖已发布 @deepseek-ai/dsh-*）
     ├─ 现成能力：agent-loop / session / tools / subagent / workflow / UI 运行时（壳 A：dsh React + void 薄 UI）/ CLI / SDK
     └─ 装载 Void 插件组（经 Void profile/bundle 组合，可整体开/关）
③ Void 插件组（树外仓库，寄生接入）
     ├─ void-core        共享配置/路径/日志/错误/生命周期（VOID_* 适配层）
     ├─ void-memory      ctx.voidMemory 能力 seam（FTS5 + sqlite-vec 知识层）
     ├─ void-tools       Star 工具注册进 ctx.tools + 策略映射
     ├─ void-legion      ctx.voidTeam 能力 seam（军团拓扑 + 权威 + 进度）
     ├─ void-channel-*   Feishu 等渠道 host/adapter
     ├─ void-browser     CDP relay seam（阶段 5）
     └─ void-security    pairing/auth/allowlist 策略（阶段 5）
```

### 11.2 Void 仓库目录结构（pnpm monorepo，树外独立仓库）

```text
void/                          # 独立 git 仓库，物理路径：E:\project\star-sanctuary\Void（位于 star-sanctuary 目录下，dsh 基座之外）
├─ package.json                # pnpm workspaces；声明 dsh.profile / dsh.bundle
├─ pnpm-workspace.yaml
├─ tsconfig.base.json          # 沿用 dsh 双 face 约定（host / client）
├─ packages/
│  ├─ void-core/               # VOID_* 配置、路径、日志、错误、来源清单、生命周期适配
│  ├─ void-memory/             # ctx.voidMemory seam（Service Definition + Provider + Consumer 工具）
│  ├─ void-tools/              # ctx.tools 注册 + tools/* 策略映射
│  ├─ void-legion/             # ctx.voidTeam seam（Service Definition + Provider + Consumer 工具 + UI slot）
│  ├─ void-channel-feishu/     # Feishu host/adapter
│  ├─ void-browser/            # CDP relay seam（阶段 5）
│  └─ void-security/           # pairing/auth 策略（阶段 5）
├─ apps/
│  └─ void-webchat/            # 独立桌面 app（Tauri 2：Rust 壳 + WebView 前端，非插件）
├─ profiles/
│  └─ void/                    # Void profile：列 Void 组合包 + cordis.patch.yml 覆盖
├─ vendor/
│  └─ star/                    # 需源码快照的 Star 模块（带来源清单 + 补丁说明）
└─ tests/                      # REAL-composition 测试 + 迁移测试 + 故障注入
```

### 11.3 关键能力 seam 契约（方法签名待 Spike 敲定）

dsh 的能力 seam 必须三角色完整（Service Definition / Provider / Consumer）。Void 新增两条核心 seam：

**`ctx.voidMemory`（知识层）**
- Service Definition：`VoidMemory` 服务，拥有 `ctx.voidMemory`；方法如 `store` / `search`（FTS + 向量混合）/ `retrieve` / `ingest`（Obsidian 等外部摄取）/ `consolidate`（dream 固化）。
- Provider：`void-memory-sqlite`（复用 belldandy-memory 的 store / indexer / reranker）。
- Consumer：`memory_search` / `memory_store` / `memory_recall` 工具 + 可选"记忆上下文"prompt section。

**`ctx.voidTeam`（军团）**
- Service Definition：`VoidTeam` 服务，拥有 `ctx.voidTeam`；方法如 `defineTeam(topology)` / `launch(team)` / `observe(team)`（roster + 权威图）/ `checkpoint(team, lane, status)`。
- Provider：`void-legion-local`（执行复用 orchestrator + launch-spec；拓扑/权威复用 delegation-protocol + team-identity-governance）。
- Consumer：`launch_legion` / `deploy_team` 工具 + 组织图 UI slot + 进度视图。

### 11.4 军团进度状态与恢复（回应 lane_progress 语义）

lane_progress 承担"人读心跳 + 机器续跑检查点"双重职责，落地为**一个真源、两个投影**：

- **权威真源 = dsh session 日志**：军团的每条进度（lane 开始 / 完成 / 验收结论 / 进度检查点）写成类型化 `voidLegion/*` 会话事件，满足 dsh 的"模型可见即已记录"不变量；断线、意外关闭后，dsh 的 session 恢复机制据此重建军团状态。
- **人读投影 = 计划文档"实施计划进度表"**：lane_progress 把日志里的进度渲染回写到计划文档（本文档唯一的进度跟踪来源）。
- **恢复语义**：崩溃后 resume 时，从日志读出各 lane 的 checkpoint，只续跑"未完成"的 lane，已完成的代码 / 验收不重做——避免大段重新查验与重复写码。

约束：进度真源只有一份（session 日志）；计划文档进度表是它的渲染投影，不允许任何 lane 各自另存一份进度（与风险表"团队拓扑真源不唯一"同一原则）。

### 11.5 复用层边界（Star 代码如何进入 Void）

| Star 模块 | Void 落点 | 复用方式（已拍板：统一源码快照） |
| --- | --- | --- |
| belldandy-memory | void-memory | 源码快照 + 来源清单（SQLite/vec 原生依赖随快照带入） |
| belldandy-skills（executor / security-matrix / runtime-policy / faqi） | void-tools | 源码快照 + 策略语义映射到 dsh sandbox / permission |
| belldandy-agent（orchestrator / launch-spec）+ belldandy-skills（delegation-protocol）+ belldandy-core（team-identity-governance） | void-legion | 源码快照（执行引擎 + 拓扑/权威类型） |
| belldandy-channels（feishu） | void-channel-feishu | 源码快照 + 传输/协议适配 |
| belldandy-browser（CDP relay） | void-browser | 源码快照（阶段 5） |
| belldandy-core（pairing / auth / allowlist） | void-security | 源码快照 + 威胁模型适配（阶段 5） |
| apps/web/public（WebChat） | apps/void-webchat | 源码快照（布局/功能模块）+ 传输层重写为 dsh SDK |

复用方式已统一拍板为源码快照（`vendor/star/` + 来源清单 + 补丁说明 + 同步测试），不把 Star 的旧负担（Belldandy 全局配置、`BELLDANDY_*` 环境、整仓依赖）带入 Void。阶段 2 PoC 用真实依赖闭包数据校准每个快照的裁剪边界（复制哪些文件、砍掉哪些依赖）。

### 11.6 配置、发行与命名空间（VOID_*）

- 发行形态：一个 `void` profile（叠加在 `dsh.base` 之后的 Void 组合包）+ 用户 `cordis.patch.yml` 覆盖，可整体开/关。
- 命名空间隔离：环境变量 `VOID_*`，数据目录 `VOID_HOME`（默认独立，不与 `DSH_HOME` / `~/.star_sanctuary` 混用），端口独立。
- 来源记录：`vendor/star/` 下每个快照带 `SOURCE.md`（来源提交、复用范围、补丁、同步测试）。

### 11.7 构建与测试拓扑

- TypeScript：沿用 dsh 双 face（host / client），tsdown 打包；Node / pnpm，依赖已发布 `@deepseek-ai/dsh-*`（阶段 1 固定精确版本）。
- 测试：REAL-composition 测试（经 Loader 装载 cordis.yml，非手搓 ctx.plugin）+ per-package invariant + HMR-safe 释放 + 故障注入 + Star 相邻测试迁移。
- 门禁：独立构建（无 Star 工作区、无绝对路径）→ 类型检查 → lint → 单测 → 最小军团端到端快照。

### 11.8 一条任务的完整数据流（端到端）

```text
用户（WebChat / Feishu）发任务
  → dsh agent-loop 领走（session 日志开 turn）
  → 模型经 ctx.tools 调 launch_legion（void-legion Consumer 工具）
  → ctx.voidTeam.launch：读 roster → 逐 lane 派发（执行走 Star orchestrator / launch-spec）
  → 每条 lane 完成时：结果写 session 日志（类型化 voidLegion/* 事件）
  → lane_progress：把日志渲染回写计划文档"实施计划进度表"
  → 组织图 UI slot 从 ctx.voidTeam 读 roster + 进度，展示依赖 / 汇报关系
  → 任务结束，turn 关闭；断线后 resume 从日志续跑未完成 lane
```

### 11.9 客户端形态与对接（apps/void-webchat）

已拍板：`apps/void-webchat` = **独立桌面 app + Tauri 2**（档③独立客户端，非插件）。这与 Star WebChat 桌面版原计划采用 Rust 的方向一致，正好延续。

- **形态**：Tauri 2 = Rust 壳（窗口 / 托盘 / 菜单 / 快捷键 / 通知 / 本地文件 / host 子进程管理）+ 系统 WebView 渲染前端。
- **前端**：WebView 内复用 Star WebChat 的布局 / CSS 做深改（功能模块不新增，只做布局与表现强化）。
- **对接（已拍板）**：**Rust 管 sidecar + 前端走 HTTP/WS 网关**——Rust 壳 spawn / 重启 dsh host 子进程（生命周期归客户端），前端 WebView 直连本机 host 的 `/api`（Typert RPC，dsh web UI 同款）；Rust 保持薄（只做壳 + 进程管理 + 系统集成）。备选"Rust 直连 stdio JSON-RPC"暂不采用（需自写 Rust JSON-RPC 客户端，官方只发 TS / Python SDK）。
- **安全**：host 只绑 `127.0.0.1`、端口独立；本地凭据 / 配对沿用 `VOID_*` 命名空间，不落系统浏览器。

### 11.10 双壳策略与开发顺序

已拍板：两个 UI 面并存，分工明确，共享同一 host 能力。

- **壳 A = dsh React 壳 + void 薄 UI**：void 插件自带的 client 半边（工具卡片、组织图 slot、进度视图、设置页），挂在 dsh 现成 React Web UI 里；只做"薄 UI"，不做 Star WebChat 的完整布局。
- **壳 B = Tauri 2 完整 UI**：`apps/void-webchat` 桌面 app，WebView 内复用 Star WebChat 布局 / CSS 做完整深改。
- **共享**：两壳消费同一 void host（RPC 契约 + session 日志真源 + 类型化事件），状态天然一致，无额外同步逻辑。
- **复杂 widget 优先 Web Component**：组织图、进度视图等复杂可视化封装成框架无关 Web Component——dsh React 壳用薄 wrapper 挂载，Tauri vanilla WebView 直接挂载，消除双写。

**开发顺序**：
1. 先做壳 A（dsh React + void 薄 UI）——这是 void 插件自带的 client 半边，最小必要路径，先让能力在 dsh 里可见可用。
2. 同时把复杂 widget 按 Web Component 落地——投资一次、两壳复用，越早定越省。
3. 待 void 真实模块积累较多、host RPC 契约稳定后，再设计开发壳 B（Tauri 2 完整 UI）。

**为什么这样排序**：壳 A 是"必须"路径（插件天然要带 client 半边），先做不浪费；Web Component 是前瞻投入，避免壳 B 阶段双写；壳 B 延后到 host 契约稳定，避免"重 UI 深改在 RPC 契约未稳时反复返工"。

**注意**：壳 A 阶段务必把复杂 widget 真正用 Web Component 落地（props / 事件契约不绑定 React 或 dsh 特定类型），否则壳 B 延后的优势会打折，组织图 / 进度视图仍会双写。

### 11.11 架构评审结论与修正（2026-08-15）

> 依据基座 `master@47f9438`（`0.1.0-rc.5`）源码与官方文档逐项核验（本人直读 + 3 个后台子代理交叉验证）。**结论：架构蓝图通过，可进入阶段 1**；但存在 1 个阻断性前提（P0）与 5 个需在阶段 1/4 固化的细化项（P1–P5）。

**基座 checkout 与第 2 节事实核对：全部通过。** 基座 = git 仓库 @ `47f9438`（仅 master、无 tag、工作区干净、origin 官方仓库）。第 2 节架构事实逐项 CONFIRMED：`ctx.agents.setFactory()` 二次注册抛错（agent-loop 单例）、`ctx.subagents` 命名多提供方注册表、`ctx.tools` 作用域化注册表 + `tools/*` waterfall、`ctx.sessions` 追加式 `SessionEvent` 日志、组合 = profile→bundle→cordis.patch.yml→home→`--patch`（按 id 整段替换/插入）、dsh 无 FTS5/sqlite-vec 知识层（仅 opt-in 会话 FTS + spill）、能力 seam 三角色 + 注册即 effect + HMR-safe、REAL-composition 测试 + per-package invariant、模型可见即已记录。

| # | 修正项 | 结论与处置 |
| --- | --- | --- |
| P0 | **版本错位（阻断）**：`0.1.0-rc.5` 未发布到 npm；公共通道仅 `latest=0.0.1-rc.1`、`next=0.1.0-rc.6`（rc.6 源码不在 master 上）。 | "依赖已发布 `@deepseek-ai/dsh-*`"按字面当前不成立。阶段 1 首动作改为消解错位：推荐方案 A（git/工作区依赖 pinned `47f9438`，`dsh plugin add github:...#47f9438` + 落地 `prepare`/`allowBuilds`）；备选 B（前移 rc.6 源码提交）；不采用 C（rc.5 源码 vs rc.6 依赖漂移）。 |
| P1 | 树外插件"client 半边"接入机制无公开文档（publish.md 仅示范 host 侧 bundle；`dsh.client` + slot 文档仅针对仓库内 `packages/client/*`）。 | 与"最小 capability seam"并列的阶段 1 验证对象；决定 11.10 双壳（壳 A）可行性。 |
| P2 | "复杂 widget 优先 Web Component"与 dsh slot 系统（React 四份 shares/hooks/zustand）存在张力，WC 无法原生消费 React 数据通道。 | 阶段 1 固化组织图/进度视图的 WC props/事件契约；正视"薄 wrapper"桥接层成本，避免双写收益打折。 |
| P3 | SDK 传输形态：官方 `dsh-sdk-client` 是 stdio NDJSON JSON-RPC（驱动子进程），非 HTTP/WS；Web UI 走 `/api`（Typert，client 在 `api/gateway/src/client`）。 | 11.9 表述已对；阶段 4 前验证 `/api` Typert client 作为第三方稳定公开面的程度。 |
| P4 | 双 face 边界：仅贡献 client 半边（壳 A 薄 UI）的插件才需 client face；纯 headless 插件（void-memory / void-legion 执行引擎）只需 host face。 | 11.7 改为"按是否含 client 半边决定单/双 face"。 |
| P5 | `verify-package-invariants` 只扫 dsh 仓库内 `packages/*/*/`，不覆盖树外 Void 包。 | 阶段 1 把"Void 侧自建等价门禁（`src/invariant.ts` + `ctx.invariants.register` + REAL-composition + HMR-safety）"列入脚手架产出。 |

**术语核对（2 处，第 2 节正文已准确、无需改）**：(1) agent-loop 单例约束实际落在 `ctx.agents` 的 `AgentFactory` 槽位（`setFactory` 二次注册抛错），`AgentLoop` 只是注册进该槽位的默认驱动；(2) Consumer 机制是 `ctx.<name>` / `ctx.get(name)`、Provider 是 `ctx.provide(name, value)` / `ctx.plugin(ServiceClass)`，**不存在 `ctx.service()`**；`ctx.tools` 的注册 API 是 `register(ToolDefinition)` / `restrict()` / `guard()`，非 `registerTool` / `ToolProvider`。

## 实施计划进度表

| 阶段 | 状态 | 风险等级 | 预期效果 | 完成条件 | 后续计划 |
| --- | --- | --- | --- | --- | --- |
| 0 方向澄清与静态评估 | 已完成 | 低 | 明确以 dsh 为基座的寄生融合路线与正确接入形态 | 第 17 章 + 本文档；dsh 基座已更新为 git 仓库 @ master@47f9438（rc.5，工作区干净） | 启动阶段 1 |
| 1 dsh 扩展契约 Spike + 树外脚手架 | 已完成（seam+tool+invariant+profile 组装，5 测试通过；含 1 项生态缺口） | 中 | 得到树外插件装载/构建/测试/清理模板 | 最小 Void 插件在干净环境独立构建、测试、装载并反向清理 | 进入阶段 2 三样本 PoC；遗留：dsh-code-runtime-worker 未发布 |
| 2 代表性 PoC（Memory/ToolExecutor/Legion） | 已完成（三样本最小闭环 + 16 测试通过） | 中高 | 用真实 Star 模块验证寄生可行性与依赖闭包，军团拓扑单真源、组织图可展示 | 三样本可装、可用、可完整释放，Star 与基座零改动 | 进入阶段 3 Void MVP |
| 3 Void MVP | 基本完成（纵向闭环端到端跑通 + 军团 launch + dream 摄取 + 渠道 seam，22 测试通过） | 高 | 交付记忆+工具+军团（公开团队 seam）+一个渠道的纵向闭环 | 达成第 8 节 MVP 完成标准 | 剩余：真实 Feishu（外部凭据）/ 全量快照 / 发行形态固化 |
| 4 UI 深改（壳 B：Tauri 2 完整 UI，布局/表现 + 寄生接入） | 未开始 | 高 | Star WebChat 布局重做、传输切到 dsh，功能模块不新增 | 桌面 app 正常启动、无新控制台错误、既有模块 DOM 可用、数据来自 dsh | 延后到 void 真实模块较多、host 契约稳定后再启动；壳 A（dsh 薄 UI）已随阶段 2/3/5 插件 client 半边完成 |
| 5 主要 Star 能力插件群 | 未开始 | 高 | 补齐 browser/渠道/安全/dream/Obsidian 等 | 每批插件有来源、生命周期、兼容测试与明确所有权 | 完成一批评估一批 |
| 6 接近 Star 功能覆盖 | 延期决策 | 极高 | 仅在 MVP 与主要插件证明价值后评估 | 需独立产品范围、预算、数据兼容与生产验收标准 | 当前不作为首期完成条件 |

---

### 阶段 1 环节实现结论：树外脚手架 + 最小三角色 seam（2026-08-15）

#### 已完成内容

1. **Void 树外仓库脚手架新建**（`E:\project\star-sanctuary\Void`）：
   - `package.json`（pnpm workspaces + `packageManager: pnpm@10.23.0`）、`pnpm-workspace.yaml`、`tsconfig.base.json`、`.gitignore`（忽略 dsh 基座与参考 checkout）。

2. **`packages/void-seam-demo` 新建**（最小三角色 seam 模板）：
   - `src/service.ts`：Service Definition（`VoidGreeterService`，声明 `ctx.voidGreeter`）；
   - `src/provider.ts`：Provider（`ctx.plugin(VoidGreeterService)`，注册即 effect、HMR-safe）；
   - `src/consumer.ts`：Consumer（`inject = ["voidGreeter"]`）；
   - `cordis.patch.yml`：`dsh.bundle` patch 层（按包名引用 provider/consumer）；
   - `tests/real-composition.spec.ts`：REAL-composition 测试（经 Loader 装载，非手搓 `ctx.plugin`）。

3. **依赖策略（绕开 P0 的关键）**：
   - 依赖已发布 cordis 家族（`@deepseek-ai/cordis@4.0.1`、`cordis-plugin-loader@1.0.2`、`cordis-plugin-include@1.0.6`），三者与基座 checkout 完全对齐——最小 seam 只需 cordis 家族，不碰 `@deepseek-ai/dsh-*` 产品包，从而把 P0 版本错位推迟到"consumer 挂 `ctx.tools`"这一环节再消解。

#### 验证结果

- TypeScript 编译无错误（`pnpm typecheck` 干净）。
- 2 个测试全部通过（vitest）：① service 提供 + consumer 装载；② provider dispose 后 service 释放、consumer 回 PENDING（HMR-safe）。
- `pnpm build` 独立构建成功（tsc → `lib/`）。

#### Spike 发现（需在后续环节跟踪）

- `FiberState` 是 `const enum`，运行时被擦除（无导出值），测试用字面量 0/2 镜像。
- **Include 并发装载竞态**：`EntryGroup.update` 以 `Promise.allSettled` 并发装载多 entry 时，provider 的 `apply()` 嵌套挂载服务与 consumer 并行加载会丢 provider fiber；改用 `loader.create()` 顺序装载则确定性通过。dsh 自身测试走子进程（`runLoaderSmoke`）可能规避此竞态——阶段 2 接 `ctx.tools` 前需确认顺序装载是否够用。

### 阶段 1 环节实现结论：consumer 工具 + invariant companion（2026-08-15）

#### 已完成内容

1. **`src/tool.ts` 新建**（Consumer 工具，真实 dsh Consumer）：
   - `inject = ["tools"]` + `ctx.tools.register(defineTool({ name: "void_greet", ... }))`——与 dsh 自带 `tool-todo` 同款形态；
   - 依赖 `@deepseek-ai/dsh-tools@0.1.0-rc.6`（`defineTool`）+ `@deepseek-ai/dsh-system-prompt@0.1.0-rc.6`（ToolRuntime 注入项）。

2. **`src/invariant.ts` 新建**（per-package invariant companion，P5）：
   - `name` / `inject = ["invariants"]` / `apply` → `ctx.invariants.register(PACKAGE_NAME, install)`；
   - `install: InvariantInstaller` 空实现 + "No runtime invariant" 理由（seam demo 无自有服务状态/事件协议）——与 `参考项目/DSH-better-sidebar-main/src/invariant.ts` 同款。

3. **测试**：
   - `tests/tools-composition.spec.ts`：`void_greet` 注册 + 模型可见（`schemas()`）+ HMR-safe 卸载；
   - `tests/invariant-composition.spec.ts`：invariant companion 经 Loader 注册（apply 解析）。

#### 验证结果

- TypeScript 编译无错误；`pnpm build` 成功。
- **5 个测试全部通过**（seam 2 + tool 2 + invariant 1）。
- P0 落地：依赖 `@deepseek-ai/dsh-*@0.1.0-rc.6`（已发布 next），生态对齐确认。

#### Spike 发现

- `defineTool.execute` 类型要求返回 `Promise`（需 `async execute`）。
- 真实 dsh 包（`dsh-tools`/`dsh-system-prompt`）经 mock `loader.internal`（Map 式 import）顺序装载确定性通过；Include 并发装载仍会触发上一条竞态。

### 阶段 1 环节实现结论：profile/bundle 组装（2026-08-15）

#### 已完成内容

1. **`@deepseek-ai/dsh@0.1.0-rc.6` CLI 安装**（Void 根 devDependency），`pnpm exec dsh` 可用（版本 rc.6）。
2. **profile 组装验证**：`dsh plugin --profile demo add ./packages/void-seam-demo` 初始化 profile（`@deepseek-ai/dsh-base` + `@void/void-seam-demo`）；`dsh --profile demo --dump-config` 正确解析出 `# == @void/void-seam-demo` 层与 provider/consumer/tool 三行（按包名 `@void/void-seam-demo/*` 引用）。
3. **干净环境核验**：包源码无绝对路径、无 `star-sanctuary`/`belldandy`/`BELLDANDY` 引用，仅依赖已发布 `@deepseek-ai/*` npm 包。

#### 验证结果

- `dsh --profile demo --dump-config` / `--dump-default-config` 均正确显示 void 三层行。
- 阶段 1 完成条件四项（独立构建 / 测试 / profile 装载 / 反向清理）全部达成。

#### Spike 发现（生态缺口，记录）

- **`@deepseek-ai/dsh-code-runtime-worker` 未发布到公共 npm**：`dsh plugin add @deepseek-ai/dsh-headless` 失败（"not in the npm registry"），阻断完整 headless 启动（`dsh --profile demo "task"`）。属 dsh 预发布 churn（风险 #1）的实证；headless 启动需待该包发布或改用源码 checkout。profile 组合（`--dump-config`）与插件装载（Loader 测试）不受影响。

### 阶段 2 样本 ① 依赖闭包勘察结论：void-memory（2026-08-15）

#### 勘察结果

1. **直接依赖包：不可行**。`@belldandy/memory` 为 `private: true`（version 0.0.0），且依赖 `@belldandy/protocol`（`workspace:*`，同为私有），二者均不在 npm。
2. **源码快照：唯一路径**，依赖闭包数据：
   - 非测试源文件 ~100 个；入口 `store.ts` 达 **4564 行**，深耦合 experience/memory-tree/task 多个子系统的 schema；
   - **`@belldandy/protocol` 耦合仅 2 个符号**：`OutboundRequestPolicy`（type）+ `resolveStateDir`/`resolveWorkspaceStateDir`（函数），散布在 7 个非测试文件（dream-model-request / openai-embedding-transport / manager / memory-chunk-summary-model-request / memory-evolution-model-request / task-summarizer / task-summary-model-request）；
   - **native/外部依赖**：`better-sqlite3`（SQLite，store 等 6 文件）、`sqlite-vec`（向量，sqlite-vec.ts）、`openai`（OpenAI SDK，embeddings/openai.ts）、`chokidar`（文件监听，indexer.ts）、`fastembed`（optional 本地 embedding，embeddings/local-provider.ts）。
3. **裁剪边界待定案**：store.ts 深耦合 experience/tree/task，"最小检索闭环剥离"的成本可能高于"全量快照 + 后裁剪"。

### 阶段 2 样本 ① 实现结论：void-memory 最小闭环（2026-08-15）

#### 已完成内容

1. **native 依赖验证**（`better-sqlite3@11.10.0` + `sqlite-vec@0.1.7-alpha.2`）：FTS5 + vec0 往返在 Windows/Void 独立构建可用。
2. **`packages/void-memory` 新建**（`ctx.voidMemory` knowledge seam 最小闭环）：
   - `src/service.ts`：Service Definition（抽象 `VoidMemory`，声明 `ctx.voidMemory` + `store`/`search`/`searchByVector`）；
   - `src/sqlite.ts`：Provider（`VoidMemorySqlite`，better-sqlite3 + sqlite-vec 实现）；
   - `src/tool.ts`：Consumer（`memory_search` 工具，`inject = ["tools", "voidMemory"]`）；
   - `src/invariant.ts`：companion；`cordis.patch.yml`：bundle 层。
3. **裁剪策略定案（偏离"源码快照"）**：PoC 用**最小重实现（~100 行）**验证 seam 形状 + native 依赖，而非快照 4564 行 store.ts——全量/选择性快照留待 seam 形状稳定后再决策。

#### 验证结果

- TypeScript 编译无错误；`pnpm build` 成功。
- 2 个新增测试通过（FTS5/vec0 往返 + HMR-safe 释放）；全仓 7 测试通过。

#### Spike 发现

- `better-sqlite3` native binding 需 `prebuild-install`（pnpm 默认拦截 build script，需 `pnpm.onlyBuiltDependencies` + rebuild）。
- `sqlite-vec` 有 Windows x64 预编译（optionalDependencies），无需编译。
- vec0 约束：rowid 必须 SQLITE_INTEGER（绑定 BigInt）；KNN 语法 `WHERE embedding MATCH ? AND k = ?`。

### 阶段 2 样本 ② 勘察结论：ToolExecutor→void-tools 语义映射（2026-08-15）

#### 勘察结果

Star 工具治理三层（belldandy-skills）：
1. **security-matrix.ts**（134 行）：channel（cli/web/browser-extension/gateway）× safe-scope（local-safe/web-safe/bridge-safe/remote-safe/privileged）访问矩阵，`evaluateToolContractAccess(tool, policy)`。
2. **runtime-policy.ts**（276 行）：`evaluateLaunchPermissionMode`（plan/acceptEdits/bypassPermissions/dontAsk/auto/confirm × needsPermission/isReadOnly/family）+ `evaluateLaunchRolePolicy`（role commander/coder/researcher/verifier × allowedToolFamilies × maxToolRiskLevel）+ `resolveRuntimeFilesystemScope`（workspace/cwd/worktree）。
3. **faqi.ts**（289 行）：per-agent 工具白名单（`faqis/*.md` 声明 toolNames + `faqis-state.json` 记录 currentFaqi）。

依赖 `ToolContract`（tool-contract.ts）：family（network-read/workspace-read/workspace-write/patch/command-exec/process-control/session-orchestration/memory/browser/service-admin/goal-governance/other）、riskLevel（low/medium/high/critical）、needsPermission、isReadOnly、channels、safeScopes。

#### 映射结论（→ dsh）

| Star 语义 | dsh 承载点 |
| --- | --- |
| ToolContract 元数据（family/risk/channels/safeScopes） | 新增 void-tools 契约注册表（dsh `ToolDefinition` 无这些字段） |
| security-matrix + faqi + 角色策略（白名单/家族/风险） | `ctx.tools.restrict(filter)`（per-scope allow/deny，天然白名单语义） |
| 权限模式（plan/acceptEdits/bypassPermissions） | `permission-presets` + `approval`（read-only/workspace-write/danger-full-access + ask/never） |
| 文件系统隔离（workspace/cwd/worktree） | `ctx.sandbox` + `ctx.fs` |
| 逐次风险检查 | `ctx.tools.guard(guard)` + `tools/pre-execute` waterfall |

#### 关键发现

- Star 三层治理大多是**纯函数**（无状态、无 disposer），映射到 dsh 的 `restrict`/`guard`（本身即 effect + disposer），"disposer 缺失"风险低。
- 唯一需新增的是 ToolContract 元数据（契约注册表）；faqi 文件状态走 `VOID_*` 目录或 dsh settings/storage。
- belldandy-skills 依赖闭包更重（~300+ 文件 + `@belldandy/memory` + puppeteer/jsdom/vscode-lsp），但治理三层零重依赖（仅 types + tool-contract 类型）——可独立快照治理层，不拖入 builtin 工具。

### 阶段 2 样本 ② 实现结论：void-tools 最小闭环（2026-08-15）

#### 已完成内容

1. **`packages/void-tools` 新建**（工具治理 seam 最小闭环）：
   - `src/contract.ts`：快照 Star tool-contract 词汇（family/riskLevel/needsPermission/isReadOnly）+ `evaluateRolePolicy` 纯函数镜像；
   - `src/registry.ts`：`VoidToolContracts` Service（契约注册表，HMR-safe）；
   - `src/policy.ts`：`ctx.tools.guard((exec) => evaluateRolePolicy(...) ? reason : undefined)`——Star 角色策略 → dsh 强制点的映射；
   - `cordis.patch.yml`：bundle 层（含示例契约 + rolePolicy）。
2. **测试**：`contract.spec.ts`（4 个纯函数单测）+ `policy-composition.spec.ts`（2 个 REAL-composition：注册表 + HMR-safe）。

#### 验证结果

- TypeScript 编译无错误；`pnpm build` 成功。
- 6 个新增测试通过；全仓 13 测试通过。

#### 关键发现

- `ctx.tools.guard` 守卫签名是 `(exec: Readonly<ToolExecution>) => string | undefined`，需 `import type { ToolExecution } from "@deepseek-ai/dsh-tools"` 触发 `ctx.tools` 声明合并。
- 映射结论落地：Star 纯函数（family × risk）经 `guard` 直落 dsh 强制点，无 disposer 缺失。

### 阶段 2 样本 ③ 实现结论：void-legion 最小闭环（2026-08-15）

#### 已完成内容

1. **`packages/void-legion` 新建**（`ctx.voidTeam` 军团 seam 最小闭环）：
   - `src/team.ts`：快照 delegation-protocol 拓扑词汇（DelegationTeamMode / Member / Metadata + 权威关系）；
   - `src/service.ts`：`VoidTeam` Service（`defineTeam` / `observe` / `checkpoint` / `getCheckpoint`，HMR-safe）；
   - `cordis.patch.yml`：bundle 层。
2. **测试**：`team-composition.spec.ts`（2 个 REAL-composition：五 lane roster + 权威图 + checkpoint；HMR-safe）。

#### 验证结果

- TypeScript 编译无错误；`pnpm build` 成功。
- 2 个新增测试通过；全仓 16 测试通过。

#### 关键发现

- `ctx.voidTeam` 以 roster + 权威关系（peer/subordinate/dependsOn）为一等数据，可被组织图 UI slot 消费——证明"不降级为 subagent provider 黑盒"。
- 执行引擎（orchestrator/launch-spec，962+375 行）留待阶段 3；PoC 只证明拓扑 seam 形状。

### 阶段 2 完成（三样本最小闭环，2026-08-15）

三样本均达成"可被 Cordis 安装（bundle+Loader）、使用（seam 方法）、完整释放（HMR-safe）、失败注入无残留（void-memory 事务回滚）、Star/dsh 零改动、无绝对路径"。全仓 16 测试通过（seam-demo 5 + void-memory 3 + void-tools 6 + void-legion 2）。

### 后续计划（阶段 2 完成 → 阶段 3）

- 下一步：进入阶段 3 Void MVP——记忆 + 工具 + 军团（公开团队 seam）+ 一个渠道的纵向闭环。
- 为什么先做它：三样本已证明寄生可行性；阶段 3 把它们串成"问→查记忆→调工具→组建军团派活"的真实任务。
- 关键闭环：① void-memory 全量快照（dream/tree/experience/task + openai→ctx.llm）待 seam 稳定后决策；② void-legion 执行引擎（orchestrator/launch-spec）复用；③ 一个渠道（Feishu）接入。
- 阶段 1 遗留：`dsh-code-runtime-worker` 未发布，完整 headless 启动待官方补发或改源码。

### 阶段 3 环节实现结论：三 seam 纵向闭环（2026-08-15）

#### 已完成内容

1. **`packages/void` 新建**（Void 组合 bundle）：`cordis.patch.yml` 把 memory/tools/legion 三层合到一个 profile 层。
2. **`tests/vertical-closure.spec.ts`**（集成测试）：同一 dsh context 装载 memory（store/search）+ tools（契约 + guard）+ legion（roster + checkpoint），并验证 `memory_search` 是唯一模型入口工具 + 全量释放后无残留。

#### 验证结果

- TypeScript 编译无错误；`pnpm build` 成功。
- 2 个新增集成测试通过；全仓 18 测试通过。

#### 关键发现

- 三 seam 经「组合 bundle + 顺序 `loader.create()`」可在同一 context 共存，形成"查记忆→调工具→组军团"闭环的 seam 基础。
- 全量 `ctx.fiber.dispose()` 后三 seam 全部逆序释放，无残留（HMR-safe 逆序释放达标）。

### 阶段 3 环节实现结论：MVP 基础设施（2026-08-15）

#### 已完成内容

1. **`SOURCES.md`**（来源清单，标准 #6）：记录 Star 只读来源（`064de90`）、dsh 基座（`47f9438`）、三插件的最小重实现来源/复用范围/Void 补丁/测试。
2. **`README.md`**（首版说明，标准 #8）：首版包含/排除、VOID_* 命名空间、升级方式、已知分叉。
3. **VOID_* 隔离**（标准 #7）：`void-memory` 的 `VoidMemorySqlite` 支持 `VOID_MEMORY_PATH`（数据目录独立于 `DSH_HOME`/`~/.star_sanctuary`）。

#### 验证结果

- TypeScript 编译无错误；`pnpm build` 成功。
- 全仓 18 测试通过（void-memory 3 测试含 VOID_MEMORY_PATH 改动后仍通过）。

### 阶段 3 环节实现结论：void-channel-feishu 渠道 seam（2026-08-15）

#### 已完成内容

1. **`packages/void-channel-feishu` 新建**（渠道 seam，mock 传输）：
   - `src/channel.ts`：`VoidChannel` 接口（脱耦 BelldandyAgent 的 Channel 快照）+ 入站消息/处理函数类型；
   - `src/service.ts`：`VoidChannels` 注册表 Service（`ctx.voidChannels`）；
   - `src/feishu.ts`：`MockFeishuChannel`（内存传输，receive→ingress→reply）；
   - `cordis.patch.yml`：bundle 层。
2. **测试**：`channel-composition.spec.ts`（2 个 REAL-composition：注册 + 消息路由 + HMR-safe）。

#### 验证结果

- TypeScript 编译无错误；`pnpm build` 成功。
- 2 个新增测试通过；全仓 20 测试通过。

#### 关键发现

- Star `Channel` 接口深度耦合 `BelldandyAgent`（Star agent runtime），不能直接复用；void 渠道改走「传输形状快照 + dsh 侧 ingress 回调」，把消息路由到 dsh agent loop。
- 真实 Feishu（Lark SDK + webhook + app 凭据）是外部依赖，MVP 首版用 mock 传输证明 seam 形状。

### 阶段 3 环节实现结论：dsh-code-runtime-worker 根因定位 + headless 启动解锁（2026-08-15）

#### 根因

`dsh plugin add @deepseek-ai/dsh-headless`（不带版本）默认装 `latest` 标签 = **`0.0.1-rc.1`**（旧版），它依赖改名前的 `@deepseek-ai/dsh-code-runtime-worker`（不带 `-thread`，从未发布到 npm）→ 悬空引用 404。`next` 标签 `0.1.0-rc.6` 才用正确的 `dsh-code-runtime-worker-thread`（已发布）。

#### 修复与验证

1. **显式版本**：`dsh plugin add @deepseek-ai/dsh-headless@0.1.0-rc.6` → 安装成功，`--dump-config` 正确组合 base + headless 层。
2. **workspace 依赖**：`@void/void` bundle 引用 `@void/void-memory/sqlite` 等，但 void 包用 `workspace:*` 内部依赖，独立 profile 无法解析 → 逐个 `dsh plugin add` link 各 void 包后解析成功。
3. **void-tools 无 config 崩溃修复**：`apply(ctx, config)` 现默认 `{}`（`contracts?`/`rolePolicy?` 可选），无 config 时不再崩。

#### 验证结果

- 完整 `dsh --profile headless "hello"` 现可**越过全部插件装载**，到达模型调用阶段，仅报 `MISSING_CREDENTIAL`（未设 `DEEPSEEK_API_KEY`）。

### 阶段 3 环节实现结论：纵向闭环端到端跑通（2026-08-15）

#### 两个必要修复

1. **Provider 默认导出 Service 类**（替换"函数插件 + 嵌套 `ctx.plugin(Service)`"）：dsh 官方 provider 是「Service 类默认导出、Loader 直接挂载」；我原先的嵌套挂载会撞上阶段 1 的 Include 并发竞态，导致 `voidMemory` 永不提供、`memory_search` 不注册。已将 `void-memory/sqlite`、`void-tools/registry`、`void-legion/service`、`void-channel-feishu/service` 改为 `export default <Service>`。
2. **`@void/void` 加入 `dsh.profile.bundles`**：`dsh plugin add <本地路径>` 只把包加为依赖（`link:`）而不自动加为 bundle（发行形态细节）；需手动把 `@void/void` 写进 profile 的 `dsh.profile.bundles`，其 patch 层才被应用。

#### 验证结果

- 设 `DEEPSEEK_API_KEY` 后，`dsh --profile headless "请调用 memory_search 搜索 'hello'"` → 模型成功调用 `memory_search` 工具并返回 `[]`（空记忆库，符合预期）。
- 全仓 20 测试通过（默认导出重构后 fiber 名改为类名，测试已同步）。

### 阶段 3 环节实现结论：belldandy-memory 全量快照（2026-08-15）

#### 已完成内容

1. **`vendor/star/belldandy-memory/` 全量快照**（workspace 包 `@void/star-belldandy-memory`）：101 个非测试源文件忠实复制 + `src/protocol/` shim（2 文件替换 `@belldandy/protocol`）。
2. **protocol 耦合处理**：`OutboundRequestPolicy` + `resolveStateDir`/`resolveWorkspaceStateDir` 本地 shim；`@belldandy/protocol` import 全部替换为本地 `./protocol/index.js`。
3. **native/外部依赖保留**：better-sqlite3 / sqlite-vec / chokidar / openai / ipaddr.js（fastembed optional）。
4. **void-memory 替换**：`VoidMemorySqlite` 改为薄封装快照 `MemoryStore`（store/search/searchByVector/ingest 委托）；SD 返回类型对齐快照（id 改 string、`score` 替代 `distance`）。

#### 验证结果

- 快照 `tsc` 编译通过（103 文件）；`MemoryStore` 运行时 store/search 往返通过。
- 全仓 22 测试通过（void-memory 4 测试含"维度变更重建无残留"）。

#### 补丁点（已文档化，未实现）

- `openai→ctx.llm`：快照暂保留 openai SDK（embedding/summary 模型调用）；`ctx.llm` 替换属后续补丁（见 `vendor/star/belldandy-memory/SOURCE.md`）。

### 阶段 3 环节实现结论：openai→ctx.llm 补丁 + 发行形态固化（2026-08-15）

#### openai→ctx.llm 补丁

1. **`requestChatCompletionViaLlm`**（`packages/void-memory/src/llm.ts`）：把 OpenAI 兼容 chat-completion payload 走 `ctx.llm.stream`，收集文本增量，返回 `{ choices: [{ message: { content } }] }`。
2. **边界**：summary/evolution/dream 的 chat-completion 可走 `ctx.llm`；**embedding 仍走 openai**（dsh 无 embedding seam，快照保留 `OpenAIEmbeddingProvider`）。
3. 验证：2 个单测（文本增量拼接 + 空流）。

#### 发行形态固化

1. **纠正误判**：`dsh plugin add <绝对路径>` **会自动识别 `dsh.bundle` 并加进 profile 的 bundles**（早前"手动补 bundle"是误判，源自错误相对路径）。
2. **脚本**：`scripts/pack-all.ps1`（打包全部到 `dist/`，`workspace:*` 自动重写为版本号）+ `scripts/install-profile.ps1`（一条命令装 headless + 4 插件包，bundle 自动识别）。
3. **已知限制**：本地 tarball 互装时相互依赖仍去 npm 解析（404）；正式发行需 `pnpm publish` 后 `dsh plugin add @void/void`。

#### 验证结果

- 全仓 24 测试通过；`pack-all.ps1` 产出 6 个 tarball；`install-profile.ps1` 一条命令完成 profile 装配且 bundle 全自动识别。

### 阶段 3 环节实现结论：P1 打通 + void-entry 入口插件骨架（2026-08-15）

#### 已完成内容

1. **P1 打通（树外插件 client 半边）**：`packages/void-entry` 按官方/参考项目模板实现：
   - `package.json` 声明 `dsh.client`（`platform: "web"` + `inject`）+ `exports["./client"]`；
   - `tsconfig.client.json`（DOM lib + `src/client/**`）+ `tsdown.config.ts`（`window.__ModuleLoader__.load({id, factory})` banner 打包）；
   - `src/client/context-types.ts`：树外插件看不到 dsh monorepo 的 cordis augmentation，故本地结构化重述 `ctx.slots`；
   - `src/client/index.tsx`：`ctx.slots.inject('settings.section', () => ctx.slots.register({...}, VoidSection))` 挂进 DSH 设置。
2. **void-entry host 半**：`VoidSuite` 服务（`ctx.voidSuite`）暴露套装目录（5 插件 + `toggleable` 标记）。
3. **验证**：`tsc + tsdown` 产出 `lib/client.js`（带正确 banner）；host 测试 1 个；全仓 25 测试通过。

#### 功能模块耦合警示（明确要求）

- **现状**：各 void 插件相互独立（关 A 不影响 B）——`void-memory` 只依赖快照，`void-tools` 只依赖 dsh-tools，`void-legion`/`void-channel-feishu`/`void-entry` 无 void 间依赖。
- **未来风险**：一旦出现 void 插件间依赖（如 `void-legion.launch` 复用 `void-tools` 契约、`void-memory.dream` 复用 `ctx.llm`），必须：① 在 `VoidSuite` 目录里声明 `dependsOn`；② 开关逻辑校验依赖——**关掉被依赖的 A 时，要么级联提示/阻止，要么明确标注会破坏 B**。
- **规则**：新增任何 void 插件间耦合，先更新 `VoidSuite` 目录的依赖声明 + 本文档的耦合清单，再写开关逻辑；禁止"静默破坏"。

#### 待续（下一轮）

- ~~总开关 + 各插件独立开关的实际逻辑~~ → 已在本轮完成（见下一节）。

### 阶段 3 环节实现结论：void-entry 开关逻辑（host 路由 + 开关状态 + client 组件）（2026-08-15）

##### 已完成内容

1. **`packages/void-entry/src/index.ts` 扩展**：
   - `VoidSuite` 新增 `setEnabled`/`getEnabled`/`setAllEnabled`，经 `ctx.loader.entries()` + `entry.update({ disabled })` 切换插件（HMR-safe）。
   - `ENTRY_IDS` 映射（pluginId → profile entry 行 id），关一个插件 = 关其全部 entry 行（耦合校验的基础）。
   - 经可选 `webServer` 注册 `/void/api/status`（读状态）+ `/void/api/toggle`（写状态）路由。

2. **`packages/void-entry/src/client/index.tsx` 扩展**：
   - 目录页改为开关组件：各插件独立 checkbox + 一键整套 checkbox（入口自身不可关）。
   - `fetch /void/api/status` 读状态、`fetch /void/api/toggle` 写状态。

3. **效果**：
   - 各插件独立开关 + 一键整套开关，关闭插件经 loader 的 `disabled` 即时生效（无需重启）。
   - 耦合规则落地：`ENTRY_IDS` 声明了插件→entry 行映射，为"关 A 校验 B"预留了校验点。

##### 验证结果

- TypeScript 编译无错误（`tsc` + `tsc -p tsconfig.client.json` + `tsdown`）。
- 全仓 27 测试通过（void-entry 3 个：目录 + 单插件开关 + 整套开关）。
- client bundle 产出 `lib/client.js`（带 `__ModuleLoader__.load` banner，含开关组件）。

##### 未验证风险

- **持久化（profile patch 写）**：`entry.update({ disabled })` 经 loader 的 `tree.write()` 回写 `cordis.patch.yml` 的机制已在 loader 源码确认，但**未在真实 profile（含 Include 插件）端到端验证文件写回 + 热重载**——REAL-composition 测试用的是 `ctx.loader.create` 直挂（无 Include，无文件写）。真实持久化需在 web 组合的 profile 里手测。

### 阶段 3 环节实现结论：军团执行引擎（launch 真实派活）（2026-08-15）

##### 已完成内容

1. **`packages/void-legion/src/service.ts` 重构**：
   - `launch` 从"占位 checkpoint"升级为真实执行引擎：按 `dependsOn` 拓扑顺序逐 lane 派发 worker。
   - 新增 `LaneWorker`/`LaneWorkerInput`/`LaunchOptions`/`LaneResult` 类型；worker 可注入（缺省 no-op）。
   - 真实进度 checkpoint（`in_progress` → `completed`/`failed`）+ 上游输出传给下游 lane。
   - 失败传播：上游 lane 失败 → 其下游直接依赖 lane 标记 `failed`（blocked）不派发。

2. **`packages/void-legion/tests` 扩展**：3 个新增测试（worker 派发顺序 + 上游输出传递 + 失败传播）。

3. **效果**：
   - `launch` 现在是"真实派活"，可挂任意 worker（后续接 `ctx.subagents`）。
   - 依赖序 + 失败传播语义清晰，符合"依赖未完成不派发下游"。

##### 验证结果

- TypeScript 编译无错误。
- 全仓 29 测试通过（void-legion 5 个：roster + HMR + 派发序 + worker + 失败传播）。

##### 待续

- worker 目前是可注入回调；接 `ctx.subagents`（真实子代理 spawn）是下一步（需要 dsh subagents seam 的树外可用性确认）。

### 后续计划（阶段 3 基本完成 → 收尾）

- 剩余（均非首版阻塞项）：① 军团 worker 接 `ctx.subagents`（真实子代理 spawn，需确认 subagents seam 树外可用）；② void-entry 开关持久化的真实 profile 端到端手测（web 组合）；③ 真实 Feishu（外部凭据，接入清单已文档化）。
