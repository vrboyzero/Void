# DeepSeek Harness 系统分析

> - 分析对象：`tmp/deepseek-harness-master` 本地源码快照
> - 分析日期：2026-08-14
> - 快照版本：`0.1.0-rc.5`
> - 项目定位：开发者预览版、MIT 开源许可

## 1. 一句话结论

DeepSeek Harness 不是一个单纯的聊天机器人，而是一套用来组装、运行和管理“能自己使用工具完成任务的 AI 助手”的开放平台。

它最突出的价值，是把模型、工具、权限、会话记录、子任务、工作流、网页界面和外部接入都做成可以替换或组合的部件。它的架构完整度和工程投入已经很高，但当前仍是开发者预览版：功能丰富、扩展能力很强，普通用户的上手成本和版本变化风险也明显高于成熟桌面软件。

综合评估为 **8.6/10**。这个分数主要肯定其架构、扩展性、安全意识和工程质量，不代表模型本身的智力评分，也不代表已经完成生产环境验证。若只评价“普通用户下载后立即使用”的成熟度，当前约为 **7.2/10**。

## 2. 普通用户可以怎样理解它

可以把 DeepSeek Harness 理解成一家可以自由组装的“AI 工作室”：

- 大语言模型负责理解目标和决定下一步。
- 文件、命令行、网页搜索等工具负责真正执行动作。
- 权限与隔离系统决定哪些动作能做、哪些动作必须先问用户。
- 会话系统保存它看过什么、说过什么、调用过什么工具。
- 子智能体和工作流系统负责把复杂任务拆开或并行处理。
- Web UI、命令行和 SDK 是不同的使用入口。
- 插件系统允许更换模型、工具、存储方式和界面功能。

因此，它更接近“AI 助手操作系统”或“智能体应用底座”，而不是一个固定功能的聊天页面。

## 3. 项目解决的核心问题

普通聊天模型只能给出建议；DeepSeek Harness 试图让模型在受控条件下完成真实工作，主要解决以下问题：

1. **让 AI 从回答问题升级为执行任务**：读取和修改文件、搜索代码、运行命令、调用网页搜索、维护计划并委派子任务。
2. **让执行过程可查看、可控制**：用户可以看到工具调用、审批请求、后台任务、子任务、计划、目标和会话轨迹。
3. **让不同能力可以替换**：例如更换模型提供方、文件系统、命令执行环境、存储方式或子智能体提供方，而不必重写整个产品。
4. **让会话和操作可以恢复与追查**：关键过程写入只追加日志，重启后可以重建对话和任务上下文。
5. **让同一个核心支持多种产品形态**：网页工作台、一次性命令行任务、Python 程序、TypeScript 程序、ACP 和 JSON-RPC 自动化接入共用同一套底层能力。

## 4. 核心机制

### 4.1 一切皆插件

项目最重要的设计原则是“一切皆插件”。模型接入、工具、会话保存、权限策略、网页界面，甚至驱动 AI 一步步工作的核心循环，都不是不可替换的固定内核。

每个插件只负责一类事情，并在装载时向系统登记自己的能力；插件卸载时，它登记的能力也会随之撤销。这使系统可以按场景增减功能，也降低了某个扩展永久污染整个运行环境的风险。

对普通用户的实际意义是：同一个程序可以组装成轻量的一次性助手，也可以组装成功能完整的网页开发工作台。

### 4.2 Profile：不同用途的整机配置

项目用 Profile 表示一套可运行的产品组合。当前发行版重点提供两种：

- **Web Profile**：启动浏览器工作台，提供会话、工作区、模型、权限、任务和插件等界面。
- **Headless Profile**：不启动网页和服务器，只接收一个任务，执行完成后把最终答案输出到命令行。

Profile 由多层配置叠加组成：先加载公共基础能力，再加载 Web 或 Headless 专属能力，最后应用用户自己的覆盖配置。这种方式灵活，但配置层会“整段替换”而不是自动深度合并，定制时需要理解被覆盖项的完整内容。

### 4.3 AI 的行动循环

一次用户任务大致按以下过程运行：

1. 用户发送目标。
2. 系统把当前会话、工作区说明、可用工具和必要上下文整理给模型。
3. 模型返回文字，或提出一个或多个工具调用。
4. 工具调用先经过权限、审批和其他规则检查。
5. 获准的工具执行，并把结果写回会话。
6. 模型读取结果，决定继续行动还是结束本轮任务。
7. 整个过程以“轮次”和“步骤”记录，直到任务完成、被阻止、报错或被用户取消。

这使它能够完成“查看项目 → 找到问题 → 修改文件 → 运行测试 → 根据结果继续修复”这样的连续工作，而不只是生成一段建议。

### 4.4 工具执行先检查、后行动、再记录

工具不是被模型直接无限制调用。一次调用会先确认工具是否存在、参数是否有效、当前会话是否有权使用，然后经过审批和安全规则，最后才真正执行。

执行成功或失败后，结果还会经过输出整理、长度控制和记录流程。大结果可以被截短或转存，避免一次工具输出挤满模型上下文。

这套流程的价值在于：权限、超时、审计、重试提醒和结果处理都可以集中应用，而不需要每个工具各写一套不一致的规则。

### 4.5 会话日志是唯一事实来源

系统不会只保存最终聊天文字，而是按顺序保存用户消息、模型输出片段、完整回复、工具调用、工具结果、轮次状态、目标变化等事件。

模型下一次看到的历史、网页回放、会话恢复、统计和追踪都从这份日志生成。项目明确要求“模型能看到的内容必须可以从日志中重建”，因此具有较好的可审计性。

如果进程在工具执行中途崩溃，恢复时不会假装工具一定成功或一定失败，而会标明“工具尚未开始”或“结果未知”，提醒后续 AI 先检查真实副作用。这比盲目重跑修改类操作更安全。

### 4.6 能力分为定义、提供者和使用者

文件系统、命令执行、网页访问、语言服务、子智能体等能力都被拆成三部分：

- 定义“这项能力能做什么”。
- 提供真正的实现，例如本地执行或远程沙箱执行。
- 提供模型可以调用的工具。

因此，更换执行环境时，模型看到的工具名称和使用方法可以保持不变。例如把文件和命令从本机切换到远程 E2B 环境，理论上不需要重写上层任务逻辑。

### 4.7 每个智能体拥有自己的能力范围

不同会话和子智能体可以使用不同的提示词、工具和限制。一个子智能体不会自动继承父级所有能力，权限也不能在自己的会话里自行扩大。

这使系统能够创建“只读审查员”“可编辑开发者”“只负责搜索的子任务”等不同角色，并减少子任务意外获得过大权限的风险。

### 4.8 长任务由目标、后台任务、子智能体和工作流共同组织

系统没有把所有长任务都塞进一种机制，而是按用途拆分：

- **Todo**：当前会话里的待办清单。
- **Plan Mode**：先制定计划并经用户确认，再进入实施。
- **Goal**：同一会话内可持续推进的单一长期目标。
- **Jobs**：后台命令、终端任务或子智能体的统一状态和控制。
- **Subagent**：把一部分工作交给另一个智能体。
- **Workflow**：由脚本安排多个子任务、并行步骤和流水线。
- **Ralph**：围绕一个固定目标，用多个全新子会话反复尝试。
- **Schedule**：在同一会话中安排延时、定时或周期性后续动作。

这种分工清晰，但也增加了普通用户理解“什么时候该用哪种任务机制”的学习成本。

## 5. 核心系统

| 核心系统 | 普通用户视角的作用 | 当前判断 |
| --- | --- | --- |
| 插件与组装系统 | 决定本次启动要装哪些能力，以及如何覆盖默认配置 | 核心基础，设计完整 |
| 智能体运行系统 | 接收目标、请求模型、执行工具并持续推进 | 核心基础，源码链路完整 |
| 模型接入系统 | 连接 DeepSeek 及其他兼容模型提供方，处理流式输出、重试和用量 | 功能丰富，模型能力仍取决于外部服务 |
| 工具与行动系统 | 管理文件、命令、终端、搜索、代码执行和其他工具 | 覆盖广，默认与可选能力并存 |
| 权限与隔离系统 | 控制读写范围、询问用户、限制子进程和保存凭据 | 安全意识强，但不是绝对安全容器 |
| 会话与恢复系统 | 保存完整过程，支持恢复、投影、查询、统计和导出 | 设计扎实，仍有存储维护限制 |
| 任务组织系统 | 管理计划、目标、后台任务、子智能体、工作流和定时动作 | 能力很强，部分状态只在当前进程存活 |
| Web 工作台 | 提供普通用户可见的聊天、设置、工作区和任务管理界面 | 功能面丰富，仍是开发者预览体验 |
| 外部接入系统 | 允许 CLI、Python、TypeScript、ACP、JSON-RPC 和 MCP 接入 | 接口路线完整，适合二次开发 |
| 插件生态系统 | 发现技能、接入 MCP、安装树外插件、动态试验插件 | 扩展潜力突出，配置与信任成本较高 |
| 诊断与工程保障 | 类型检查、测试、覆盖率、文档同步、依赖和发布检查 | 工程纪律非常突出 |

## 6. 功能模块罗列与说明

### 6.1 用户入口

- **Web UI**：默认通过 `npx @deepseek-ai/dsh web` 启动，提供本地网页工作台。
- **Headless CLI**：用一条命令提交一次性任务，适合脚本和自动化流程。
- **Python SDK**：Python 程序可以启动随包附带的运行时并执行任务。
- **TypeScript SDK / JSON-RPC**：JavaScript 或其他进程可以通过稳定消息协议控制智能体。
- **ACP 服务**：面向支持 Agent Client Protocol 的自动化客户端。

### 6.2 Web 工作台

- 工作区选择与管理。
- 多会话导航和聊天输入。
- 模型提供方、模型与默认模型设置。
- 权限模式切换和审批界面。
- 工具调用过程及专用结果卡片。
- 附件和图片展示。
- 计划、目标、待办事项和用户问题。
- 后台任务状态和输出。
- 子智能体状态、跳转和会话记录。
- 工作流运行记录。
- 会话轨迹、统计、导出和消息反馈。
- 插件设置、插件清单、主题和语言。

### 6.3 模型接入

- DeepSeek 官方模型接入。
- Anthropic、OpenAI 等目录提供方。
- OpenAI 兼容的企业网关或自建端点。
- 模型选择、推理强度、最大输出和重试策略。
- 文本与图片输入能力声明。
- Token 使用量计量。

密钥采用“引用”方式管理：设置中保存的是密钥位置，网页只能得到已配置/来源等脱敏描述，不会读取明文密钥。

### 6.4 文件与代码操作

- 读取文本文件和图片。
- 创建、写入、插入和替换文件内容。
- 使用 `glob` 和 `grep` 查找文件及内容。
- 在修改前要求先观察文件，并可通过版本信息避免覆盖刚刚被别人改过的内容。
- 使用语言服务器查找定义、引用、实现和悬浮信息。
- 以 Code Mode 编写一小段 TypeScript 或 Python 程序，组合调用多个已有工具。

### 6.5 命令与终端

- Linux/macOS 使用 Bash，Windows 使用 PowerShell。
- 支持一次性命令和后台命令。
- 支持持久终端，会保留工作目录、变量和交互状态。
- 支持列出、读取、发送输入、发送信号和关闭终端。
- 支持任务超时、进程树清理和统一后台任务控制。

### 6.6 网络与外部信息

- 支持 DeepSeek、Exa、Perplexity 等网页搜索提供方。
- 提供统一的网页搜索工具，切换提供方时上层用法不变。
- 项目也包含网页抓取实现，但发行版基础组合默认关闭抓取，因为其 SSRF 防护仍被明确延期。SSRF 可以理解为“恶意地址诱导服务器访问内网或敏感服务”的风险。

### 6.7 计划与持续任务

- AI 可以维护结构化 Todo 清单。
- Plan Mode 支持先提交完整计划给用户审批。
- Goal 保存同一会话的长期目标、状态、修订号和轮次上限。
- Schedule 支持延时、指定时间和固定周期的会话内后续动作。
- Compaction 在对话过长时压缩历史，并优先修剪过大的工具输出。

### 6.8 子智能体与并行协作

- 在同一进程中新建独立子智能体。
- 从父会话的一段历史派生子会话。
- 接入 Codex、Claude Code、ACP 或 DSH SDK 作为子智能体提供方。
- 支持前台等待或后台执行。
- 支持列出、发送后续消息和中断可继续的子智能体。
- 子智能体完成后，可以把总结投递回父会话。

当前限制是部分所有权、邮箱和继续执行状态仍只存在于当前进程；进程崩溃时，尚未写入会话日志的消息可能丢失，跨进程协作也还不是完整的持久调度系统。

### 6.9 工作流与多轮尝试

- 模型可以编写工作流脚本，按顺序、并行或流水线方式调用子智能体。
- 工作流引擎限制并发数、总子任务数和数据格式。
- Ralph 模式为同一目标启动多轮全新子会话，通过工作区和结构化交接继续推进。

当前工作流没有中间检查点，进程重启后不能从中间继续；也没有跨子智能体的统一 Token 或费用预算。

### 6.10 技能、MCP 与插件

- 从本地文件发现并加载可复用 Skill 指令。
- 通过 MCP 客户端接入外部工具服务。
- 通过 Profile 安装树外插件并覆盖默认配置。
- 提供 Codex 和 Claude Code 钩子桥接。
- 可选的 Cordis 动态工具允许 AI 在当前进程中试验新的 Host 或网页插件。

动态插件属于高级能力，其隔离只用于约束正常代码，不构成强安全边界，应当按“允许执行命令”同等级别谨慎授权；它也不会跨进程重启保存。

### 6.11 会话、存储与查询

- 只追加的会话事件日志。
- JSONL 文件存储，可选 Zstandard 压缩。
- SQLite 存储。
- 会话恢复、崩溃尾部修复和中断状态标记。
- 会话标题、统计、遥测、投影缓存和导出。
- 可选的会话全文搜索、事件读取、事件关系和会话血缘查询。
- 附件使用内容寻址方式保存，便于复用和校验。

当前没有内置的会话删除和保留期接口；JSONL 文件或 SQLite 行会持续积累，需要外部维护。SQLite 后端还使用同步访问，繁忙多会话场景可能受到吞吐限制，并且数据库写锁没有等待或重试策略。

### 6.12 设置、凭据与身份

- 用户设置保存在本地文件并支持热更新。
- API 密钥与普通设置分离保存。
- 环境变量和 `.env` 可以作为只读凭据来源。
- 网页只显示密钥是否已配置，不返回明文。
- 支持匿名用户标识，用于不包含个人信息的共享身份场景。

### 6.13 隔离与审批

- `read-only`：只读文件，敏感动作需要询问。
- `workspace-write`：可以写当前工作区和会话临时目录，敏感动作需要询问；这是基础组合的默认值。
- `danger-full-access`：使用运行进程本身的全部权限，并默认不询问。

本地隔离会按平台选择 Linux `bwrap`/Landlock、macOS Seatbelt 或 Windows ACL 受限令牌。请求了隔离但宿主无法提供时，系统选择拒绝执行，而不是静默取消限制。

需要注意：这些机制主要限制文件访问，不等于完整的网络、内核或多租户安全容器。Python SDK 的最小示例明确使用 `danger-full-access`，只适合可丢弃的代码副本或容器。

## 7. 默认可用、可选能力与试验能力

### 7.1 发行版基础组合重点提供

- DeepSeek 模型与其他模型路由。
- 文件读写、搜索和替换编辑。
- Bash 或 PowerShell 命令。
- Web 搜索。
- Todo、Goal、Plan、后台任务、Subagent、Workflow 和 Ralph。
- 会话保存、压缩、设置、凭据、权限与审批。
- Web UI 或 Headless 两类使用方式。

### 7.2 需要额外配置或选择启用

- 持久终端。
- LSP 代码语义导航。
- 会话全文检索和面向模型的会话查询。
- Schedule 定时任务。
- MCP 外部工具服务。
- Claude Code、Codex、ACP 和 DSH SDK 子智能体。
- E2B 远程执行环境。
- 网页抓取。
- 运行时自修改的 Cordis 工具集。

### 7.3 明确仍有试验或延期边界

- E2B 在包清单中标为 POC。
- 项目整体处于开发者预览，未来允许破坏兼容性的改动。
- 会话格式版本仍为预发布 `v0`，不承诺通用迁移。
- 动态工作流不支持重启恢复。
- 多项后台任务和子智能体状态仍是单进程范围。
- Web 抓取的 SSRF 防护尚未闭合，因此默认关闭。
- 自修改插件的 VM 隔离不是安全边界。
- 模型图片能力依赖人工声明，系统不会主动验证远端是否真的支持。

## 8. 项目规模与工程证据

本次静态盘点得到：

- `219` 个 `packages/<group>/<package>` 工作区包。
- 约 `2477` 个位于 `packages/`、`apps/`、`scripts/` 下的 TypeScript/TSX 文件。
- `829` 个测试文件，其中 `684` 个位于 `packages/`。
- `215` 篇 `docs/` Markdown 文档。
- 根目录提供单元测试、覆盖率、端到端测试、快照回放、Web 测试、类型检查、代码检查、重复代码检查、依赖闭包、文档同步和多平台检查脚本。
- 源码采用严格类型检查，并对包导出、文档、配置目录、工具目录、模块图和许可证设置机器检查。

这些数字说明项目投入很大、工程治理较系统，但不能单独证明所有功能都已在真实用户环境成熟运行。

## 9. 评分与评估

### 9.1 分项评分

| 维度 | 评分 | 评价 |
| --- | ---: | --- |
| 核心架构 | **9.4/10** | 插件化贯彻到模型、工具、会话和执行循环，职责拆分清晰 |
| 扩展能力 | **9.5/10** | Profile、能力提供方、Skill、MCP、SDK、UI 插槽和外部子智能体形成完整扩展路线 |
| 功能完整度 | **9.0/10** | 编程智能体所需的工作区、命令、文件、计划、并行、会话和 UI 基本齐全 |
| 安全设计 | **8.6/10** | 默认工作区写入、审批、凭据脱敏、隔离失败关闭做得较好；仍非完整安全容器 |
| 可靠性设计 | **8.5/10** | 只追加日志、崩溃标记、flush 和清理纪律较强；工作流及部分并行状态不能跨进程恢复 |
| 工程质量 | **9.1/10** | 测试、类型、文档、生成目录和发布检查覆盖广，规则执行严格 |
| 用户体验 | **7.6/10** | Web 功能丰富，但概念和配置很多，尚不属于零学习成本产品 |
| 安装与部署 | **7.2/10** | npm 入口简单，但源码构建、Node 版本、平台隔离和可选后端仍有环境要求 |
| 版本成熟度 | **6.5/10** | `0.1.0-rc.5` 开发者预览，明确允许破坏兼容和拒绝旧存储格式 |

### 9.2 综合结论

- **综合项目评分：8.6/10**。
- **架构与二次开发价值：9.3/10**。
- **普通用户当前就绪度：7.2/10**。
- **直接用于关键生产任务的就绪度：7.0/10**，需要团队自行完成版本冻结、安全配置、备份、监控、容量和回滚验证。

它已经是一套内容丰富、结构严谨的智能体平台源码，但还不是“安装后无需理解权限和运行环境即可放心托管重要工作的成熟消费级产品”。

## 10. 主要优点

### 10.1 架构原则统一

“一切皆插件”不是口号，主循环、模型、工具、存储和界面都遵循同一组合方式。统一原则使新能力有明确归属，也便于裁剪不用的部分。

### 10.2 对真实行动的覆盖非常完整

文件、命令、持久终端、代码语义导航、网页搜索、后台任务、子智能体和工作流形成了完整的执行链，而不是只有聊天和少量演示工具。

### 10.3 可审计性强

系统保存模型看到的上下文、工具调用、工具结果和任务状态。流式输出也被记录，网页回放、恢复和模型历史使用同一来源，减少“界面显示”和“模型实际经历”不一致的问题。

### 10.4 安全策略相对务实

默认不是无限制访问；密钥不会回传到网页；隔离不可用时拒绝执行；子智能体不能自行扩大权限。项目还明确记录动态插件、网页抓取和全权限 SDK 示例的风险，没有把不完整隔离包装成绝对安全。

### 10.5 多入口共用同一核心

Web、Headless、Python、TypeScript、ACP 和 JSON-RPC 不是互相独立的重复产品，而是共享同一套智能体、会话和工具能力。这有利于从人工操作逐步升级到自动化集成。

### 10.6 工程治理成熟度高

源码、文档、工具目录、配置目录、模块图和发布产物之间设置了大量自动一致性检查。大量测试不仅关注正常功能，也覆盖取消、崩溃、清理、跨平台和错误输入。

### 10.7 适合构建不同形态的智能体产品

服务定义与具体实现分离，UI 也有插件插槽。团队可以在不复制核心代码的前提下，替换模型、执行环境、存储后端和用户界面组件。

## 11. 主要缺点与风险

### 11.1 对普通用户仍然复杂

Profile、Bundle、Preset、Plugin、Tool、Goal、Job、Workflow、Subagent 等概念很多。虽然各自职责清楚，但首次使用者需要先理解模型、工作区和权限，深度定制者还要理解配置层和插件树。

### 11.2 预发布版本兼容风险高

项目明确允许破坏兼容性，旧磁盘格式可能直接被拒绝，且当前会话格式没有通用迁移承诺。升级前必须备份数据并在副本中验证，不能按成熟长期支持产品的方式无条件滚动升级。

### 11.3 模块数量带来维护成本

219 个包使职责边界清晰，也带来依赖图、构建、发布和排错复杂度。小团队二次开发时，理解成本和跨包变更成本都不会低。

### 11.4 部分“长任务”仍只在当前进程可靠

工作流没有检查点，后台任务和部分子智能体控制状态没有跨进程协调。进程退出后，持久会话可以恢复，但一个正在运行的复杂并行任务未必能从原位置继续。

### 11.5 安全能力有明确边界

本地沙箱主要控制文件访问，不等于完整网络隔离或强多租户容器；动态 Cordis 代码可能触达真实运行时；`danger-full-access` 会使用进程本身全部权限。生产环境仍需容器、独立账号、网络策略和可丢弃工作区等外部防护。

### 11.6 存储维护能力尚不完整

没有内置会话删除和保留策略；SQLite 同步访问可能阻塞服务，写争用没有等待与重试。长期、多用户或高并发部署需要另行设计容量管理和备份恢复。

### 11.7 部分能力是“已有模块”而非“默认可用”

持久终端、LSP、会话查询、MCP、E2B、网页抓取和外部子智能体需要额外配置，有的仍为 POC 或带延期项。评估时不能把仓库里存在的包全部视为开箱即用功能。

### 11.8 缺少可直接引用的项目级效果基准

根 `BENCHMARK.md` 只有如何运行基准的简短说明，没有给出统一任务集、成功率、成本、耗时或竞品对比结果。因此本分析只能评价系统设计和工程证据，不能证明它在真实编码任务上优于其他智能体平台。

## 12. 适用场景

### 12.1 很适合

- 需要搭建内部 AI 编程工作台的研发团队。
- 需要自由替换模型、工具或执行环境的平台团队。
- 研究智能体循环、工具治理、子任务和工作流的开发者。
- 需要同时提供网页人工操作和 SDK 自动化的产品。
- 能接受开发者预览状态，并有能力自行测试、备份和加固的技术团队。

### 12.2 当前不太适合

- 只想下载一个无需配置的聊天软件的普通消费者。
- 无技术人员维护、但要求长期稳定和数据无损升级的组织。
- 直接处理生产密钥、生产数据库或不可逆操作，而没有容器与审批制度的场景。
- 要求跨机器、跨进程可靠调度大量并行任务的成熟任务平台场景。
- 需要已有权威基准证明效果、成本和 SLA 的采购决策。

## 13. 建议的后续优先级

1. **先稳定版本与数据迁移策略**：为会话、设置和插件配置提供明确升级路径、备份检查和回滚说明。
2. **降低首次使用门槛**：增加环境自检、权限解释、推荐预设和失败修复向导，把高级概念留到需要时再展示。
3. **补齐长任务跨进程恢复**：为工作流、后台任务和可继续子智能体增加持久检查点、租约和可靠邮箱。
4. **完善存储生命周期**：增加会话删除、保留期、归档、容量统计、SQLite 争用处理和备份恢复工具。
5. **继续收紧安全边界**：闭合网页抓取 SSRF 防护，隔离动态插件，提供容器化和网络限制的官方部署模板。
6. **建立可复现效果基准**：公开固定任务、环境、成功率、耗时、Token、费用和失败分类，区分模型能力与 Harness 贡献。
7. **建立生产运行证据**：补充长时间运行、并发会话、崩溃恢复、跨平台安装和版本升级测试报告。

## 14. 最终评价

DeepSeek Harness 展现出的不是一个轻量实验项目，而是一套试图完整解决智能体产品工程问题的平台：如何组装能力、怎样让模型行动、如何限制权限、怎样保存事实、如何拆分任务，以及如何让网页和 SDK 共享同一运行核心。

它最强的部分是架构一致性、扩展性、会话审计和工程纪律；最需要继续补齐的是预发布兼容、普通用户上手、跨进程长任务恢复、存储生命周期和可复现效果基准。

如果把它当成一个可二次开发的智能体平台，它已经很有竞争力；如果把它当成一个面向所有普通用户的成熟成品，则还需要经过版本稳定化、部署加固和产品体验收敛。

## 15. 分析依据与边界

本次重点查验了以下本地文件和目录：

- `README.zh.md`、`package.json`、`pnpm-workspace.yaml`
- `AGENTS.md`、`docs/architecture.zh.md`、`docs/glossary.md`
- `docs/user/guide/` 用户指南
- `docs/tool-catalog.zh.md`
- `packages/README.zh.md` 与各能力组 README
- `packages/bundle/base/cordis.patch.yml`
- `packages/bundle/web-app/cordis.patch.yml`
- `packages/bundle/headless/cordis.patch.yml`
- `packages/core/agent-loop/src/agent.ts`
- `packages/core/tools/src/index.ts`
- `packages/core/session/src/index.ts`
- `apps/cli/src/bin.ts` 与 `apps/cli/src/profile-boot.ts`
- `apps/web/`、`python/`、`native/`、`examples/` 和测试目录

目标目录没有 `.git` 元数据，也没有 `node_modules`，因此本次无法确认具体提交 SHA，且没有在该快照内执行安装、构建、测试或真实模型任务。所有功能判断均来自当前源码、配置、项目文档和测试文件的静态交叉核验；运行性能、模型效果、成本和生产稳定性仍需在目标环境中单独验证。

## 16. 以 Cordis 为核心建设 Void、复用 Star Sanctuary 模块的可行性评估

### 16.1 评估前提

本节评估的目标不是改造或迁移现有 Star Sanctuary，而是：

1. **新建独立项目 Void**；
2. **以 DeepSeek Harness 所使用的 Cordis 为 Void 的组合与生命周期核心**；
3. Star Sanctuary 保持原项目、原架构和原运行路径不变；
4. Void 单向复用 Star Sanctuary 的代码、设计和测试资产；
5. 所有 Cordis Adapter、兼容层、复制后的修改和 Void 专属实现都放在 Void 仓库中，不反向侵入 Star；
6. 尽量延用 Star 原有实现，但不强求每段代码都原样导入，也不以“复用率”牺牲 Void 的模块接口和长期可维护性。

两者的关系应定义为：

```text
DeepSeek Harness 的 Cordis
            |
            v
     Void 组合与生命周期核心
            |
     +------+------+------+------+------+
     |      |      |      |      |      |
   Agent  Tooling Memory Work Channels Gateway ...
     ^      ^      ^      ^      ^
     |      |      |      |      |
     +--- 从 Star Sanctuary 选择性复用实现 ---+

Star Sanctuary：独立保留、只读参考或固定版本代码来源
Void：独立构建、独立发布、独立配置、独立持久化
```

这意味着本节不再讨论“替换 Star 的组合根”“在 Star 内保留新旧启动路径”或“逐步 Cordis 化 Star”。需要评估的是：Star 各模块能否被提取到 Void、需要多少 Void 侧适配、如何防止复制代码长期分叉，以及 Cordis 是否适合承载这些插件。

### 16.2 核心结论

**总体可行性为中高，建议实施有闭包边界的 Void PoC；但把 Star 所有大大小小功能都原样变成插件，代价仍然较大。**

关键判断如下：

| 判断项 | 结论 |
| --- | --- |
| 用 Cordis 作为 Void 的组合与生命周期核心 | **可行性高**。Cordis 的 Context、Service、插件树和 effect 适合从零建立组合规则 |
| 保持 Star Sanctuary 完全不变 | **可行**。Cordis 适配代码全部放在 Void，不要求 Star 反向依赖 Void |
| 原样复用 Star 的叶子模块和资源型模块 | **可行性中高**。显式构造、依赖较少且已有 start/stop/close 的模块最合适 |
| 原样复用 Agent、Conversation、Gateway 等核心模块 | **可行性中等或偏低**。可复用大量实现，但通常需要携带较大的类型和依赖闭包 |
| 每个类、文件、工具都做成顶层 Cordis 插件 | **不推荐**。会产生大量浅模块、配置碎片和复杂插件图 |
| 用父插件组织领域、用子插件承载确需独立开关的能力 | **推荐**。既能覆盖“大大小小功能”，又不把内部实现全部暴露 |
| 建成可用的 Void MVP | **中高成本**。Cordis 本身不是主要工作量，Star 代码提取、契约适配和测试迁移才是 |
| 达到接近 Star 的完整功能覆盖 | **高成本**。即使复用代码，也接近一次独立产品工程建设 |

最合适的方向不是“把 Star 的目录逐个翻译成 Cordis 插件”，而是“先定义 Void 的稳定接口，再为这些接口选择 Star 实现作为 Adapter”。这样复用的是已经验证的行为和深层实现，而不是把 Star 当前的所有耦合一并复制到 Void。

### 16.3 Cordis 在 Void 中应该负责什么

Cordis 适合承担：

- Void 根 Context 和插件树；
- Cordis Service 的提供、消费和作用域管理；
- 插件安装、启动、停止和 effect 反向清理；
- 按运行形态组合 Headless、Web、渠道或本地能力；
- 配置注入、能力发现和可选插件启停；
- 受信任进程内插件之间的生命周期协调。

Cordis 不会自动提供：

- Star 的 Agent 行为、ConversationStore、工具策略、记忆检索和渠道协议；
- DeepSeek Harness 的 Agent Loop、Session、RPC、工作流或 Web UI；
- 不可信插件的安全隔离；
- Star 与 Harness 两套状态模型之间的数据兼容；
- 任意旧代码的自动卸载、自动回滚或安全 HMR。

DeepSeek Harness 的 Cordis 快照中，`vendor/cordis/src/fiber.ts` 直接负责插件执行与 effect，`vendor/cordis/src/service.ts` 和 `vendor/cordis/src/reflect.ts` 提供 Service 注册机制。这些能力足够构成 Void 的组合核心，但 Cordis 仍只是核心基础设施，不等于完整 Harness 运行时。

还需注意，DeepSeek Harness 的 Web 与 Headless 默认组合本身禁用了 HMR。Void 首阶段应承诺“可预测启动、停止和失败清理”，不应承诺生产插件热重载。

### 16.4 推荐的 Void 插件架构

Void 顶层应保持约 `8-12` 个粗粒度深模块。每个深模块通过较小的接口隐藏 Star 实现、配置转换、资源所有权和错误处理；真正需要独立启停的渠道、提供者、工具集合或后台能力，可以作为其子插件。

| Void 顶层插件模块 | 主要责任 | 可选子插件示例 | Star 代码来源 |
| --- | --- | --- | --- |
| `void-foundation` | Void 配置、日志、路径、标识、错误与运行环境 | 配置源、日志输出、诊断 | core 中的配置与 doctor 逻辑可选择性参考，不直接沿用 Star 命名空间 |
| `void-tooling` | 工具注册、执行、策略、hook 和审计 | builtin tools、MCP tools、浏览器工具 | `belldandy-skills`、Agent hooks |
| `void-agent` | Agent 创建、模型调用、运行循环和能力装配 | 模型提供者、Profile、特定 Agent | `belldandy-agent` |
| `void-conversation` | 会话、消息、恢复和持久化真源 | SQLite、内存测试实现、迁移工具 | ConversationStore 及相邻实现 |
| `void-knowledge` | 记忆、检索、索引和知识摄取 | FTS、向量检索、文件摄取 | `belldandy-memory` |
| `void-work` | 目标、任务、子任务、工作流和工作区 | background job、scheduler、project | Agent 与 Core 的工作管理实现 |
| `void-browser` | Browser Relay、浏览器连接和扩展协作 | relay、extension bridge | `belldandy-browser`、浏览器扩展相关实现 |
| `void-channels` | 渠道抽象、路由、消息归一化与生命周期 | Feishu、其他渠道 Adapter | `belldandy-channels` |
| `void-extension-host` | Void 插件发现、Skill、外部扩展和隔离监督 | OCI runtime、skill loader | Star 现有插件与 ExtensionRuntimeSupervisor |
| `void-gateway` | Void 的 HTTP/WebSocket/RPC、认证和外部宿主 | Web host、headless host、local host | 选择性复用 `belldandy-core`，不直接照搬宽依赖对象 |
| `void-ui` | Void Web 客户端及 UI 扩展插槽 | Chat、doctor、settings、task detail | WebChat 可作为行为和局部代码来源 |
| `void-observability` | 插件状态、诊断、指标和故障信息 | health、trace、doctor | Star 日志和 doctor 能力可选择性复用 |

插件层次建议采用：

- **父插件定义领域接口和共同生命周期**；
- **子插件只用于真实可选、可替换或有独立资源生命周期的功能**；
- 内部算法、工具辅助函数和只被一个模块使用的类继续作为普通实现，不为了形式变成插件；
- 插件不等于独立 npm 包。多个紧密相关的子插件可以放在同一 Void 包中，避免形成与 DeepSeek Harness 相同量级的包管理负担。

判断一个 Star 功能是否值得成为 Cordis 插件时，应满足至少一项：

1. 需要按 Void 发行形态启用或禁用；
2. 拥有需要 Cordis 管理的独立资源生命周期；
3. 存在至少两个真实 Adapter，例如生产实现和内存测试实现；
4. 需要被其他多个插件通过稳定接口消费；
5. 配置、权限或故障需要独立隔离和诊断。

否则应留在所属深模块内部。

### 16.5 Star 代码的三种复用方式

Star 不修改，并不意味着 Void 只能复制代码。可以按模块选择三种方式：

| 方式 | 做法 | 优点 | 代价与适用范围 |
| --- | --- | --- | --- |
| 固定版本包依赖 | Void 依赖由 Star 固定提交构建出的包或制品 | 原代码零复制，后续升级来源明确 | 只适合导出稳定、依赖闭包可控的包；不得依赖开发机绝对路径 |
| 源码快照/vendor | 把选定源码和相邻测试复制到 Void，记录来源提交和本地补丁 | Star 保持不变，Void 可独立构建并按需适配 | 需要持续同步、安全审计和许可证/版权记录 |
| Void 原生移植 | 在 Void 按自己的接口重新组织实现，复用 Star 算法、行为和测试用例 | 长期接口最干净，不携带 Star 全部耦合 | 初始成本最高，代码级复用率最低，适合 Agent/Gateway 等高耦合区域 |

推荐采用混合策略：

1. 对导出稳定且依赖闭包小的包，优先使用固定版本制品；
2. 对实现成熟但无法独立发布的叶子模块，使用带来源记录的源码快照；
3. 对深度依赖 Star 全局配置、类型和组合根的核心模块，在 Void 内移植，而不是强行原样导入；
4. 所有 Star 兼容逻辑放在 Void 的 `star-adapter` 或对应插件内部；
5. 依赖方向只能是 `Void → 固定 Star 制品/源码`，不得出现 `Star → Void`。

每个复用插件应记录：

- 来源仓库和提交；
- 复制或依赖的文件/包；
- 保持原样的部分；
- Void 补丁及其原因；
- 一并迁移的测试；
- 上游同步状态和已知分叉。

这套来源清单是长期维护 Void 的必要资产，否则“尽量沿用原代码”会迅速演变为无法追踪的复制代码。

### 16.6 各类 Star 模块的可复用性与移植代价

| Star 区域 | 原实现复用潜力 | Void 适配代价 | 主要原因 | 推荐方式 |
| --- | --- | --- | --- | --- |
| 纯函数、Schema、协议类型、解析与校验逻辑 | 高 | 低 | 无资源生命周期，依赖通常较小 | 固定包依赖或带测试源码快照 |
| Browser Relay | 高 | 低到中 | 已有 `start()` / `stop()`，生命周期清晰 | 优先作为 Cordis effect PoC |
| MemoryManager | 中高 | 中 | 已有 `close()`，但绑定存储路径、索引配置和本地资源 | 在 `void-knowledge` 中用 Adapter 包装 |
| ChannelManager 与单个渠道 | 中高 | 中 | 管理入口明确，但配置、重连、认证和消息类型存在依赖 | 父插件加渠道子插件 |
| 内置工具与 ToolExecutor | 中高 | 中 | 工具逻辑可复用，但执行上下文、权限、hook 和注册清理相互关联 | 迁移完整工具执行接缝，不逐函数拼装 |
| OCI 扩展运行时 | 中高 | 中到高 | 隔离实现有价值，但依赖进程、路径、权限和插件协议 | 作为整体 Adapter 移植，保留安全语义 |
| 工作、目标、子任务和后台任务 | 中 | 中到高 | 依赖 Agent 状态、会话、工具和事件传播 | 先形成 `void-work` 深模块，再选择性复用内部实现 |
| AgentRegistry 与 Agent Runtime | 中 | 高 | 与模型、工具、会话、记忆和运行配置交叉依赖 | Void 原生接口加 Star 实现移植 |
| ConversationStore | 中 | 高 | 可复用实现，但会把会话真源、恢复语义、数据路径和类型带入 Void | 先决定 Void 唯一会话模型，再选择 Adapter |
| Gateway Server / RPC / Auth / Pairing | 中低 | 高 | `GatewayServerOptions` 依赖面宽，协议与 Star 产品形态绑定 | Void 原生宿主，选择性复用深层逻辑 |
| WebChat | 中低 | 高 | 强依赖 Star Gateway RPC、DOM 接线和设置语义 | 等 Void 协议稳定后再移植或适配 |
| CLI、环境变量和运行目录 | 低到中 | 中 | 含 Star 品牌、`BELLDANDY_*` 配置和 `~/.star_sanctuary` 状态约定 | Void 自行实现命名与路径，复用解析/doctor 逻辑 |

上述“复用潜力”指实现和行为可延用程度，不代表文件可以不经检查直接复制。实际可复用率必须由 PoC 统计依赖闭包、必要补丁和测试通过情况后确定，当前不能可靠给出百分比。

### 16.7 “Star 不修改”下的最小改动原则

正确的最小改动策略是：

- Star Sanctuary 仓库零修改；
- Void 不通过写入 Star 源码来完成适配；
- 能直接依赖的 Star 包保持原样；
- 必须修改的复制代码只在 Void 内形成受控分叉；
- Cordis Service 暴露 Void 自己的接口，不把 Star 的宽类型和全局配置直接变成公共接口；
- Void Adapter 负责配置名、路径、日志、错误、生命周期和类型转换；
- 外部协议兼容只在确有产品需求时保留，不默认复制全部 Star RPC；
- Void 使用独立的环境变量、数据目录、端口默认值、插件目录和运行身份，避免与 Star 同机运行时互相污染。

尤其需要避免以下“表面零修改”方案：

1. Void 直接引用 Star 仓库内部文件和开发机绝对路径；
2. 把整个 Star Gateway 启动函数作为一个 Cordis 插件运行；
3. 把 `GatewayServerOptions` 或全局配置对象整体注册为 Cordis Service；
4. 为了不改复制代码而在 Adapter 中不断堆叠隐式全局变量；
5. 同时使用 Star ConversationStore 和 Harness Session 作为会话真源；
6. 复制源码但不复制相邻测试、许可证信息和来源提交。

这些做法短期看似复用率高，实际只是把耦合和维护成本推迟到 Void。

### 16.8 风险等级与主要失败模式

总体风险为 **中高**，但风险分布与“改造 Star”方案不同：

- 对 Star Sanctuary 的回归风险：**低**，因为原项目不修改；
- Void PoC 的可回滚性：**高**，失败时停止 Void 即可；
- Void 的交付进度和长期维护风险：**中高到高**；
- 追求 Star 近完整功能覆盖时的范围风险：**高**。

| 失败模式 | 影响 | 早期信号 | 控制措施 |
| --- | --- | --- | --- |
| 直接导入一个模块却拉入大半个 Star | Void 实际变成 Star 的隐式镜像，构建和升级失控 | 一个插件依赖大量未使用包、环境变量和启动代码 | PoC 必须输出传递依赖闭包；超过合理范围时改用源码快照或原生移植 |
| 复制代码来源失踪 | 无法判断安全修复和上游变化是否已同步 | 文件没有来源提交，补丁与原代码混在一起 | 为每个插件维护来源清单、补丁说明和同步测试 |
| Void 接口泄漏 Star 内部类型 | 后续无法替换实现，Cordis 只剩转发作用 | 调用方大量 import `belldandy-*` 内部类型 | Void 定义稳定接口，Star 类型只存在于 Adapter 内部 |
| 插件粒度过细 | 插件树、配置和测试比原系统更复杂 | 大量插件只有一两个转发函数 | 顶层限制为粗粒度深模块，小功能作为内部实现或必要子插件 |
| 注册无法反向清理 | 重装后工具、hook、监听器和定时器重复 | 第二次创建 Context 时行为次数增加 | Void Adapter 建立 disposer/注册账本，做重复安装和故障注入测试 |
| 会话或任务出现双真源 | 恢复、UI 和模型历史不一致 | 同一动作写入两套事件或数据库 | Void 在 PoC 后立即确定唯一会话与任务真源 |
| 配置和运行目录与 Star 冲突 | 两个项目同机运行时互相读取或覆盖状态 | Void 仍使用 `BELLDANDY_*` 或 Star 默认目录 | 使用独立 `VOID_*` 命名空间、目录和端口 |
| 把 Cordis 当作安全沙箱 | 不可信插件获得 Void 宿主权限 | 动态代码可访问文件、网络和进程 | Cordis 只运行受信任插件；复用 OCI 监督器或另建外部隔离 |
| Cordis vendor 与上游漂移 | 修复难同步、插件契约不稳定 | Void 补丁持续增加且无法对应上游版本 | 固定版本/提交，记录本地补丁，建立升级兼容套件 |
| 只复制实现、不迁移测试 | Star 中已解决的回归在 Void 重现 | 插件只有启动 smoke，没有错误和恢复测试 | 相邻测试与实现一起评估，改为从 Void 模块接口验证可观察行为 |
| “全部功能插件化”无闭包 | Void 长期停留在半成品迁移 | 不断新增清单但没有可交付纵向工作流 | 先定义 MVP 行为和明确排除项，按纵向闭环交付 |
| Star 与 Void 长期双线演进 | 相同缺陷修复两次，行为逐渐分叉 | 同一模块出现无法自动合并的不同补丁 | 明确哪些模块跟随 Star、哪些已由 Void 接管所有权，不维持模糊中间态 |

最大的长期成本不是第一次复制，而是之后 Star 修复 bug 或改变模块时，Void 要判断是否同步以及如何重新验证。

### 16.9 可行性前置条件

开始 Void 实施前至少需要完成：

1. **确定 Void 产品闭包**：首个版本需要哪些用户行为，哪些 Star 功能明确延期；
2. **确定 Cordis 来源策略**：使用 DeepSeek Harness vendor 快照、上游包还是 Void 自维护 fork，并核对许可证和补丁；
3. **确定唯一状态模型**：Agent、Conversation、Task/Work 分别采用 Star、Harness 还是 Void 原生设计，不允许长期双写；
4. **定义 Void 插件接口规范**：Service 命名、作用域、启动/停止、错误模式、配置校验和 disposer 契约；
5. **建立源码来源机制**：固定 Star 提交、来源清单、补丁目录、许可证/版权记录和同步流程；
6. **隔离运行命名空间**：使用 `VOID_*`、Void 数据目录、独立端口和独立插件目录；
7. **建立测试迁移规则**：复用实现时同步评估相邻测试，在 Void 模块接口处补生命周期和故障测试；
8. **确定安全分层**：受信任 Cordis 插件与不可信外部扩展使用不同执行路径；
9. **建立独立构建要求**：Void CI 不依赖开发机上的 `E:\project\star-sanctuary` 绝对路径，依赖必须可固定和获取；
10. **确认代码使用权**：复制或发布 Star 与 Cordis 代码前，确认适用许可证、内部版权和分发要求。

### 16.10 粗略工作量估算

以下估算基于这些假设：

- Star Sanctuary 始终只读；
- Void 使用 TypeScript/Node/pnpm，并允许复用 Star 源码和测试；
- 首阶段不要求兼容 Star 既有用户数据；
- 不同时重做完整 Web UI；
- 有 `2-3` 名熟悉 Star 和智能体运行时的工程师；
- 估算为工程量级，不是固定排期承诺。

| 范围 | 粗略工作量 | 主要产出 |
| --- | ---: | --- |
| 提取盘点与依赖闭包分析 | `1-2` 工程师周 | 候选模块、传递依赖、许可证、测试和复用方式清单 |
| Cordis 技术 Spike | `1-2` 工程师周 | Void 最小宿主、Service/effect、作用域、配置与错误传播验证 |
| 代表性 PoC | **累计 `4-8` 工程师周** | ToolExecutor、MemoryManager、Browser Relay 等真实 Star 模块插件及独立构建验证 |
| Void MVP | **累计 `5-9` 工程师月** | Agent、会话、工具、记忆、Headless Gateway、一个渠道和基本诊断的纵向闭环 |
| 主要 Star 能力插件群 | **累计 `10-18` 工程师月** | 工作管理、浏览器、渠道群、扩展宿主、认证、后台能力和兼容测试 |
| 接近 Star 功能覆盖的独立产品 | **累计 `18-30+` 工程师月** | WebChat/扩展、完整渠道、安全与恢复、安装升级、文档和生产加固 |

相比直接在 Star 内改造，新建 Void 的优势是 Star 风险低、架构选择自由、回滚简单；代价是必须重新建立宿主、配置、构建、发布、诊断和集成测试，并承担复制代码同步成本。对于高耦合核心模块，新项目方案的初期工程量可能反而更高。

不能在 PoC 前给出可信的“可复用代码百分比”。PoC 应记录每个样本模块的直接复用文件、必要修改、传递依赖和迁移测试数量，用真实数据校准后续估算。

### 16.11 闭包边界与完成标准

#### 本提案包含

- 新建独立 Void 项目并以 Cordis 作为组合与生命周期核心；
- 设计 Void 自己的模块接口和插件树；
- 从固定版本 Star 选择性依赖、复制或移植实现与测试；
- 在 Void 内建立 Star Adapter、来源追踪和同步机制；
- 建立独立配置、数据目录、构建、测试、诊断和发布路径；
- 通过父插件和必要子插件承载 Star 的大、小功能；
- 保留不可信扩展的进程外或容器隔离；
- 用纵向工作流逐步达到 Void 产品目标。

#### 本提案明确不包含

- 修改 Star Sanctuary 源码、配置、测试或运行方式；
- 要求 Star 添加 Cordis 接口或为 Void 发布专用兼容代码；
- 默认兼容 Star 既有数据目录、会话数据库和全部 RPC；
- 把每个 Star 文件或类机械转换成独立插件；
- 自动同步 Star 的所有后续提交；
- 把 Cordis 当作安全沙箱；
- 在 PoC 阶段迁移 WebChat、全部渠道或完整 Gateway；
- 当前即承诺与 Star 功能完全等价。

#### Void MVP 完成标准

1. Void 可以在没有 Star 工作区和绝对路径的干净环境中独立安装、构建、测试和启动；
2. Star Sanctuary 工作树在整个实施过程中保持无本任务改动；
3. Cordis 是 Void 模块实例和生命周期的唯一组合核心；
4. 至少形成 Agent、Conversation、Tooling、Knowledge、Gateway 和一个 Channel 的完整用户工作流；
5. 插件可独立启停，失败时已创建资源按逆序释放，重复创建 Context 不产生残留；
6. Void 使用独立配置、数据目录、端口和插件目录，不会污染 Star；
7. 每个 Star 来源插件都记录来源提交、复用范围、Void 补丁、测试和同步状态；
8. 会话与任务各自只有一个事实来源，崩溃恢复和错误路径通过集成验证；
9. 进程内 Cordis 插件仅限受信任代码，不可信扩展仍经过隔离；
10. 文档明确首版包含项、排除项、升级方式和已知分叉。

### 16.12 推荐 PoC

PoC 应回答四个问题：

1. Star 模块在不改 Star 的情况下，能否被 Void 独立获取和构建；
2. Void Adapter 能否把 Star 的构造、配置和生命周期转换成 Cordis 插件；
3. 原实现的传递依赖和必要补丁是否在可接受范围；
4. 插件卸载、启动失败和重复创建时是否能够完整清理。

建议选择三个代表性样本：

| 样本 | 代表的问题 | 选择原因 |
| --- | --- | --- |
| Browser Relay | 显式启动/停止资源 | 生命周期清楚，可验证 Cordis effect 的基础价值 |
| MemoryManager | 本地持久资源与配置转换 | 可验证路径隔离、关闭、测试替身和存储依赖 |
| ToolExecutor 加一个真实工具 | 注册、策略、hook 和执行上下文 | 可暴露 disposer 缺失与跨模块类型耦合 |

PoC 实施范围：

1. 在新的 Void 仓库创建最小 Cordis Host；
2. 建立 `void-foundation`、`void-browser`、`void-knowledge` 和 `void-tooling`；
3. 优先尝试固定版本包依赖；不适合时再使用有来源记录的源码快照；
4. 由 Void Adapter 完成 `VOID_*` 配置、路径、日志、错误和生命周期转换；
5. 使用测试 Host 让三个插件发生真实交互，而不是只验证加载成功；
6. 测试正常启动/停止、重复安装/卸载、中途失败、插件缺失和清理后再次启动；
7. 输出每个样本的依赖闭包、原样复用范围、Void 修改量、迁移测试和未解决风险；
8. 在无 Star 本地工作区的环境中执行独立构建和测试。

PoC 通过门禁：

- Star 仓库没有任何为 Void 产生的源码改动；
- Void 不包含指向 `E:\project\star-sanctuary` 的绝对路径；
- 三个样本均能由 Cordis 安装、使用和完整释放；
- 失败注入后不存在重复工具、hook、监听器、端口、数据库句柄或未退出进程；
- 复制代码均有来源记录，依赖制品均固定到明确版本或提交；
- 至少一个样本保持原实现直接复用，一个样本验证 Void Adapter 能吸收配置和类型差异；
- 团队能够用实际数据判断后续模块应直接依赖、源码快照还是原生移植；
- PoC 未把整个 Star Gateway 或大半个 monorepo 作为隐式依赖带入 Void。

若 PoC 发现代表性模块都必须携带 Star 的全局配置、Gateway 组合根和大量内部包才能工作，应停止追求“原样代码复用”，转为复用行为、测试和设计，在 Void 中实现原生深模块。Cordis 方案本身仍可能成立，但预期成本必须上调。

### 16.13 最终建议

建议批准的不是“把 Star 迁移到 Cordis”，而是：

> **新建 Void，以 Cordis 为组合核心；把 Star 视为只读的实现来源，通过 Void 自己的 Adapter 和深模块接口选择性复用。先完成代表性 PoC，再决定功能覆盖范围。**

这条路线的主要价值是：

- Star Sanctuary 不承担实验性架构改造风险；
- Void 从第一天获得统一插件树、生命周期和发行组合能力；
- 成熟的 Star 实现、行为和测试可以减少重复开发；
- Void 可以在不兼容 Star 历史架构的地方做更干净的选择。

主要代价是：

- 需要维护 Void 与 Star 的单向来源关系和受控分叉；
- 核心模块不能保证原样复用；
- Void 仍需独立建设宿主、配置、持久化、诊断、发布和产品集成；
- “所有功能插件化”必须分阶段闭环，否则容易演变为长期无完成定义的复制工程。

技术债决策：

- Void Cordis PoC：`split_task`，作为独立新项目的第一项架构验证；
- Star 模块提取与来源追踪：`split_task`，PoC 前建立最小规则；
- Tool/hook 注册缺少 disposer：`fix_now` 仅指 Void Adapter 内解决，Star 原代码不改；
- Star Gateway 整体复用：`defer`，MVP 宿主接口确定后再评估；
- Star/Harness 会话双模型：`record_only` 风险，实施前必须二选一；
- Star 近完整功能覆盖：`defer`，MVP 通过后按产品价值逐批决策。

## 17. 直接以 deepseek-harness 为基座、把 Star 做成一整套 Void 插件的可行性评估

> 本章是对第 16 节路线的替代评估。前提从"新建独立 Void + 自建 Cordis"改为"直接用 deepseek-harness 作基座 + 树外 Void 插件组"。第 16 节及其结论保持原样；本章独立陈述新路线的可行性、风险与工作量。

### 17.1 评估前提

1. **基座** = `deepseek-harness-master` 本身，保持 checkout 零改动，不 fork、不改 `packages/`、不改 `vendor/`。
2. **Void** = 一个树外插件仓库，通过 `dsh.profile` / `dsh.bundle` 与已发布的 `@deepseek-ai/dsh-*` 组合；基座能力（agent-loop、session、tools、subagent、workflow、UI、CLI/SDK）全部沿用 dsh 现成实现。
3. **复用方向**严格单向 `Void → Star`，Star 仓库零修改。
4. **"整套 Star 插件组"的目标**：Star 各功能以 dsh 的能力 seam 身份接入，可整体安装/卸载，并尽量保留 Star 自己的表达（工具、记忆、渠道、浏览器、安全策略）。
5. 复用方式沿用三种：固定版本依赖 / 源码快照(vendor) / Void 原生移植。

### 17.2 核心结论

**总体可行性为中高到高，明显优于第 16 节"自建 Cordis"路线**：宿主、agent-loop、session、tools、subagent、workflow、UI、CLI/SDK 全部现成，Void 只做"能力接入与适配"，省去最大的一块自建工程。

但"整套 Star 插件组自带 agent-loop/session 且与 dsh 不冲突"这半句需要纠正。dsh 的扩展点分两类：

- **注册表语义**（多提供方共存）：`ctx.tools`、`ctx.subagents`、`ctx.llm`、workflow provider、commands、jobs、prompt sections、session persistence backend、UI slots、skills、spill backend、telemetry。Star 对应能力挂上去即可，天然不冲突。
- **单例 Service 语义**（只能替换，不能并存）：`ctx.agentLoop`（`setFactory()` 已注册即抛错）、`ctx.sessions`（唯一追加式日志真源）、`ctx.systemPrompt`、session title、agent default model。

因此推荐的寄生形态是：**Star 的能力以 dsh 的 seam 身份接入 dsh 的 loop/session/tools/subagent/workflow/UI，而不是另起一套 loop/session**。这既实现"整组开关"，又避免双真源与核心替换的代价。

### 17.3 本次勘察确认的关键架构事实

| 事实 | 结论 | 对 Void 的含义 |
| --- | --- | --- |
| `ctx.agentLoop` 是单例（`@deepseek-ai/dsh-agent-loop`），`ctx.agents.setFactory()` 已注册时抛错 | agent-loop 只能有一个 | Star 不能"并存"自己的 loop，只能替换或走 subagent provider |
| `ctx.subagents` 是命名多提供方注册表（`registerProvider`/`list`） | 子智能体后端可共存 | Star 的 belldandy-agent 可作为新增 `star` provider 寄生接入 |
| `ctx.tools` 是作用域化工具注册表 | 工具可叠加 | Star 的 belldandy-skills 工具与 dsh 工具共存 |
| `ctx.sessions` 是唯一追加式 `SessionEvent` 日志真源 | 会话只能有一个真源 | Star 的 ConversationStore 不得作为第二真源 |
| `ctx.llm` 是适配器注册表 | 模型提供方可共存 | Star 的 FailoverClient 可作为 adapter 或保持独立进程 |
| 组合 = profile → bundle → cordis.patch.yml → home → --patch，按 id 整段替换 config 或插入 | 可关闭/替换任何一行 | 可整体开/关 Star 插件组，也可替换某一行 |
| dsh 无 FTS5/sqlite-vec 向量检索与知识记忆能力（仅 opt-in 会话全文检索 + spill） | Star 记忆是纯增量差异化 | MemoryManager 是最干净的新 capability seam |
| 能力 seam 必须三角色完整（Service Definition / Provider / Consumer），注册是 effect 且 HMR-safe | 接入门槛比裸 Cordis 高 | 每个 Star 能力都要按 seam 契约完整设计 |

### 17.4 对"寄生融合"想法的判断：注册表 vs 单例

**能寄生共存的部分**：tools、subagent providers、workflow providers、llm adapters、commands、jobs、prompt sections、session persistence backends、UI slots / ConversationNodeDefinition、skills、spill backends、telemetry。这些是注册表语义，Star 对应能力挂上去即可，天然不冲突。

**不能并存、只能替换的部分**：agent-loop（`ctx.agentLoop`）、session 真源（`ctx.sessions`）、system-prompt 组装（`ctx.systemPrompt`）、session title（唯一 provider）、agent default model。这些是单例 Service 语义。

结论：方向正确，但应把"Star 自己带 agent-loop/session"重新表述为"**Star 的 Agent 能力以 subagent provider（或 ACP/JSON-RPC 远端）身份接入，Star 的记忆以 knowledge service 身份接入**"。这样既保留 dsh 的 loop/session/goal/ralph/todo/plan/compaction 全套产品能力，又让 Star 的差异化能力真正寄生融合、互不冲突。

### 17.5 Star 能力的差异化分层

| 类别 | Star 模块 | 处理建议 |
| --- | --- | --- |
| 冗余（dsh 已有且更成熟） | agent-loop、ConversationStore、todo/plan/goal/subagent/workflow、bash/pwsh/fs/glob/grep、web search、compaction、skill | 不移植或仅薄适配，直接使用 dsh 对应能力 |
| 纯增量差异化 | belldandy-memory（FTS5+vec 混合 RAG、记忆树、dream 固化、Obsidian 摄取） | 新建 knowledge capability seam，最高优先级 |
| 差异化传输/宿主 | belldandy-channels（Feishu 等）、belldandy-browser（CDP relay） | 新 host/adapter 或新 capability seam |
| 差异化安全 | pairing/auth/allowlist（belldandy-core） | 评估 dsh 的 identity/credentials/permission/approval 是否够用，不够则作为新 seam |
| 差异化工具 | belldandy-skills 的 doctor、subtask detail 等 | 按需注册到 `ctx.tools` |

### 17.6 推荐的 Void 插件形态（Star 模块 → dsh seam）

| Void 插件 | dsh 承载点 | 角色 |
| --- | --- | --- |
| `void-memory` | 新 `ctx.voidMemory` Service Definition + SQLite Provider + `memory_*` Consumer 工具 | 新 capability seam |
| `void-tools` | `ctx.tools` 注册 + `tools/*` waterfall + `ctx.sandbox`/`ctx.permission` | Consumer + 策略 |
| `void-agent` | `ctx.subagents.registerProvider('star')` | subagent provider |
| `void-channel-feishu` | 新 host/adapter + MessageSource | 宿主/传输 |
| `void-browser` | 新 `ctx.browser` seam + 工具 | 新 capability seam |
| `void-gateway-security` | 复用/扩展 credentials/permission/approval | 策略 provider |

### 17.7 MVP/PoC 三样本落点与验证目标

1. **MemoryManager → knowledge seam**：验证"纯增量能力"从零接入 dsh 的全流程（Service Definition/Provider/Consumer + cordis.yml + profile/bundle + 独立构建 + HMR-safe 释放 + 模型可见即已记录）。三个里最干净、最该先做。
2. **ToolExecutor → `ctx.tools` + `tools/*`**：验证 Star 工具执行语义（security-matrix/runtime-policy/faqi）能否映射到 dsh 的 sandbox/approval/permission/scope-restriction，暴露 disposer 缺失与跨模块类型耦合。
3. **belldandy-agent → `ctx.subagents` 注册 `star` provider**：验证"寄生"路径——把 Star 的 agent runtime 作为 dsh 可委派的子智能体后端，而不是替换 agent-loop；同时记录"若替换 agent-loop 会失去什么"的对照证据。

### 17.8 风险等级与主要失败模式

总体风险 **中**（低于第 16 节的"中高"，因为不再自建宿主），但对基座变更的暴露上升。

| 失败模式 | 影响 | 早期信号 | 控制措施 |
| --- | --- | --- | --- |
| dsh 预发布 breaking change 直接波及插件 | Void 插件随基座升级频繁返工 | 类型/运行时契约随 `0.1.0-rc.*` 升级而破坏 | 固定精确版本/提交，维护升级兼容测试套件，不跨大版本静默升级 |
| 把 Star 能力误接成单例替换 | 失去 dsh 的 goal/ralph/todo/plan/compaction 或造成双真源 | 尝试替换 `agent-loop` 行或并行写两套 session | 遵循 17.4 分层，loop/session 一律走注册表语义接入 |
| Star 工具执行语义与 dsh 策略层不一致 | 权限、hook、审批在 dsh 下表现不同 | 同一工具在 Star 与 dsh 下行为不一致 | 逐工具映射 security-matrix/runtime-policy → sandbox/approval/permission/scope-restriction |
| 直接导入 Star 模块拉入大半个 Star | Void 插件依赖闭包失控 | 一个插件拖入大量未用包、`BELLDANDY_*` 环境变量 | PoC 输出传递依赖闭包；超范围改用源码快照或原生移植 |
| 注册无法反向清理 | 重装后工具、监听器重复 | 重复创建 Context 行为次数增加 | 遵循 dsh 的 effect/HMR-safe 契约，补重复安装与故障注入测试 |
| dsh 严格契约抬高接入成本 | 每包 invariant、seam 完整性、测试策略让"真插件"变重 | 反复在 dsh 的 gate（invariant/typecheck/lint）上返工 | 先吃透 docs/cookbook + packages/AGENTS.md，按 seam 三角色一次设计到位 |
| 复用代码来源失踪 | 无法判断 Star 修复与上游是否同步 | 复制文件无来源提交、补丁混写 | 每插件维护来源清单、补丁说明、同步测试 |
| 配置/数据目录与 dsh 或 Star 冲突 | 同机运行互相污染 | 复用 `DSH_*`/`BELLDANDY_*` 或默认目录 | Void 用独立 `VOID_*` 命名空间、数据目录、端口、插件目录 |

### 17.9 可行性前置条件

1. 确定 Void 插件组的发行形态：一个 `dsh.profile` 还是若干 `dsh.bundle`，以及如何被用户安装。
2. 固定 dsh 精确版本/提交，并建立升级兼容测试；接受预发布 breaking change 的成本。
3. 确定唯一状态模型：会话真源用 dsh 的 `SessionEvent` 日志；Star 记忆作为其上的 knowledge 层，不双写。
4. 吃透并遵守 dsh 契约：capability seam 三角色、`ctx.effect`/`ctx.on`、branded id、declaration merging、HMR-safe、模型可见即已记录、per-package invariant。
5. 建立 Star 来源机制：固定提交、来源清单、补丁目录、许可证/版权记录、同步流程。
6. 隔离运行命名空间：`VOID_*` 环境变量、独立数据目录、端口、插件目录，不与 dsh/Star 冲突。
7. 建立测试迁移规则：复用实现时同步评估相邻测试，在 Void 模块接口处补生命周期与故障测试（dsh 要求 REAL-composition 测试，非手搓 `ctx.plugin`）。
8. 确定安全分层：受信任进程内 Cordis 插件与不可信外部扩展（OCI/远端）走不同路径。

### 17.10 粗略工作量估算

假设：Star 始终只读；Void 用 TypeScript/Node/pnpm 并依赖已发布 dsh 包；首阶段不兼容 Star 既有用户数据；有 2-3 名熟悉 Star 与 dsh 的工程师；估算为工程量级，非排期承诺。

| 范围 | 粗略工作量 | 主要产出 |
| --- | ---: | --- |
| dsh 扩展契约与测试策略 Spike | `1-2` 工程师周 | 树外插件脚手架、一个最小 seam、cordis.yml/profile 组装、独立构建与 HMR-safe 验证 |
| 代表性 PoC | **累计 `4-8` 工程师周** | MemoryManager、ToolExecutor、belldandy-agent（subagent provider）三个真实样本及依赖闭包数据 |
| Void MVP | **累计 `4-7` 工程师月** | 记忆检索 + 一组 Star 工具 + Star agent 作为可委派后端 + Feishu 渠道，全部跑在 dsh 的 loop/session/UI 上 |
| 主要 Star 能力插件群 | **累计 `8-15` 工程师月** | browser CDP、更多渠道、pairing 安全、dream/Obsidian 摄取、更多工具 |
| 接近 Star 功能覆盖（含自建/深改 UI） | **累计 `14-24` 工程师月** | WebChat 或深度改造 dsh UI、完整渠道、安全与恢复、安装升级、生产加固 |

相比第 16 节，节省主要来自"宿主/loop/session/tools/subagent/workflow/UI/CLI/SDK 现成"，成本则转移到"dsh 严格契约遵守 + Star 执行/安全语义映射 + 预发布 churn"。核心模块仍不能保证原样复用；PoC 前不能给出可信的"可复用代码百分比"。

### 17.11 闭包边界与完成标准

#### 本方案包含

- 以 dsh 为基座、树外 Void 插件组承载 Star 差异化能力；
- 遵守 dsh 的 seam/组合/生命周期契约，Star 零修改、基座零改动；
- 复用方式三选一，配来源清单与同步机制；
- 独立 `VOID_*` 命名空间、数据目录、构建、测试、发布路径。

#### 本方案明确不包含

- fork 或修改 deepseek-harness 基座；
- 修改 Star 源码、配置、测试或运行方式；
- 替换 dsh 的 agent-loop 或 session 真源（保留 dsh 产品能力）；
- 默认兼容 Star 既有数据目录、会话数据库与全部 RPC；
- 把每个 Star 文件或类机械转换成独立插件；
- PoC 阶段即迁移 WebChat、全部渠道或完整 Gateway。

#### Void MVP 完成标准

1. Void 在干净环境（无 Star 工作区、无绝对路径）独立安装、构建、测试、并以 dsh profile 方式启动；
2. Star 工作树与 dsh 基座 checkout 全程无本任务改动；
3. 形成 Memory、Tools、Agent（subagent provider）三者的完整纵向闭环；
4. 插件可独立启停，失败按逆序释放，重复创建 Context 不产生残留（HMR-safe）；
5. 会话真源只有 dsh 的 SessionEvent 日志，Star 记忆不双写；
6. 每个 Star 来源插件记录来源提交、复用范围、Void 补丁、测试与同步状态；
7. 进程内 Cordis 插件仅限受信任代码，不可信扩展仍走隔离；
8. 文档明确首版包含项、排除项、升级方式与已知分叉。

### 17.12 与第 16 节路线对比与建议

| 维度 | 第 16 节（自建 Cordis） | 第 17 节（以 dsh 为基座） |
| --- | --- | --- |
| 宿主/loop/session/tools/UI | 需自建 | 现成，Void 只做接入 |
| 对 dsh 预发布变更的暴露 | 低（vendor 固定快照） | 高（依赖已发布包） |
| 接入契约强度 | 裸 Cordis，较自由 | dsh seam/不变式/测试策略，较严格 |
| Star 核心模块复用 | 移植到 Void 原生接口 | 以 seam 身份接入，或 subagent provider |
| 会话/状态真源 | 需自定 | 直接用 dsh，Star 记忆作为知识层 |
| 主要风险 | 自建宿主工程大 | 契约遵守 + 语义映射 + churn |
| 建议 | 备选 | **首选** |

建议批准的不是"把 Star 迁移进 dsh 内核"，而是：**新建树外 Void 插件组，以 dsh 为基座、遵守其 seam 契约接入 Star 的差异化能力；先做 17.7 的三样本 PoC，再决定功能覆盖范围。**

## 实施计划进度表

| 阶段 | 状态 | 风险等级 | 预期效果 | 完成条件 | 后续计划 |
| --- | --- | --- | --- | --- | --- |
| Void 方向澄清与静态评估 | 已完成 | 低 | 明确 Star 保持不变、Void 独立建设及 Cordis 的正确定位 | 第 16 节按新项目模型完成范围、风险、成本与门禁评估 | 由项目负责人确认是否批准 Void 提取盘点 |
| Void 产品闭包与状态模型决策 | 待决策 | 中高 | 明确 MVP 行为、唯一会话/任务真源和明确排除项 | 形成 Void MVP 清单及 Star/Harness/Void 实现选择表 | 先决策状态模型，因为它决定 Agent、Conversation 和 Work 接缝 |
| Star 模块提取盘点 | 未开始 | 中 | 用真实依赖闭包替代主观复用率估计 | 完成候选包、传递依赖、测试、许可证和复用方式清单 | 选择三个代表性 PoC 样本并固定来源提交 |
| Cordis 来源与技术 Spike | 未开始 | 中 | 验证 Cordis 版本策略、Service、effect、作用域和独立构建 | 形成可运行最小 Void Host、补丁记录和已知限制 | 通过后接入代表性 Star 模块 |
| 代表性 PoC | 未开始 | 中高 | 验证 Star 零修改、Void 独立构建和真实插件生命周期 | 达成 16.12 的全部门禁，输出实际复用与修改数据 | 据数据决定直接依赖、源码快照和原生移植比例 |
| Void MVP | 未开始 | 高 | 交付 Agent、会话、工具、记忆、Gateway 和一个渠道的纵向闭环 | 达成 16.11 的 MVP 完成标准 | 根据用户价值扩展 Work、Browser、Channels 和 Extension Host |
| 主要 Star 能力插件群 | 未开始 | 高 | 逐步获得 Star 的成熟能力，同时保持 Void 插件接口独立 | 每批插件都有来源、生命周期、兼容测试和明确所有权 | 完成一批评估一批，不预先承诺全部迁移 |
| 接近 Star 功能覆盖 | 延期决策 | 极高 | 仅在 Void MVP 和主要插件证明价值后评估 | 需要独立产品范围、预算、数据兼容和生产验收标准 | 当前不作为 Void 首期完成条件 |
