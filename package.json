{
  "name": "void",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.23.0",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "gen:primitives": "node scripts/gen-primitives-types.mjs",
    "check:primitives": "node scripts/gen-primitives-types.mjs --check",
    "verify:star-memory-snapshot": "node scripts/verify-star-memory-snapshot.mjs"
  },
  "devDependencies": {
    "@deepseek-ai/dsh": "0.1.0-rc.6"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
